eBPF进程监控系统 - 本科毕业答辩问题与答案

======================================
第一部分：项目概述与背景 (1-10)
======================================

1. 问：请简要介绍一下你的毕业设计项目。
答：我的毕业设计是基于eBPF技术的Linux进程安全监控系统。该系统利用eBPF（Extended Berkeley Packet Filter）在内核层面实时监控进程的创建、文件操作、权限变更和网络连接等行为，通过风险分析引擎识别潜在的安全威胁，如权限提升、反向shell攻击等，并提供可视化仪表板和取证报告功能。

2. 问：你为什么选择eBPF作为核心技术？
答：选择eBPF主要基于以下原因：（1）eBPF可以在不修改内核源码的情况下，在内核空间安全地执行自定义程序；（2）性能开销小，可以在生产环境中运行；（3）提供了丰富的hook点，可以监控系统调用、LSM钩子等多种事件；（4）生态成熟，是Linux内核监控的主流技术方向。

3. 问：这个项目解决了什么实际问题？
答：项目解决了传统安全监控系统的几个痛点：（1）传统用户态监控容易被绕过；（2）审计日志分析滞后，无法实时响应；（3）缺乏对权限提升等高级攻击的检测能力；（4）取证困难，无法重建攻击时间线。本系统实现了内核级实时监控，能够及时发现威胁并生成详细取证报告。

4. 问：系统的主要功能模块有哪些？
答：系统主要包含四个模块：（1）Agent模块：负责加载eBPF程序并从内核采集事件；（2）Server模块：接收事件、存储数据、进行风险分析；（3）风险分析模块：评估事件风险等级，识别攻击模式；（4）前端仪表板：提供实时事件展示、查询和取证报告功能。

5. 问：项目的创新点是什么？
答：主要创新点包括：（1）综合使用tracepoint和LSM hook，覆盖更全面的监控场景；（2）实现了基于事件特征的风险评级系统；（3）支持攻击时间线重建和取证报告生成；（4）采用了事件去重机制，避免重复告警。

6. 问：系统的应用场景有哪些？
答：系统适用于多种场景：（1）服务器安全监控，实时检测入侵行为；（2）合规审计，满足等保2.0等安全要求；（3）应急响应，快速定位攻击源头；（4）安全研究，分析恶意软件行为；（5）DevSecOps，将安全监控集成到开发流程中。

7. 问：与传统的安全工具相比，你的系统有什么优势？
答：优势包括：（1）内核级监控，更难被绕过；（2）实时性强，亚秒级事件捕获；（3）上下文信息丰富，包含进程关系、权限信息等；（4）自动化风险分析，减少人工干预；（5）支持取证溯源，满足事后分析需求。

8. 问：开发这个项目用了哪些技术栈？
答：技术栈包括：（1）C语言：编写eBPF内核程序；（2）Go语言：开发Agent和Server；（3）JavaScript/HTML：开发前端仪表板；（4）SQLite：存储安全事件数据；（5）Linux内核API：进程信息获取、权限监控等。

9. 问：项目的开发周期是多久？
答：项目开发周期约为X个月，经历了需求分析、技术调研、系统设计、编码实现、测试优化和文档撰写等阶段。其中eBPF程序调试和风险算法优化花费时间最多。

10. 问：你在项目中负责哪些工作？
答：我独立完成了整个项目的开发工作，包括：eBPF程序设计与实现、Agent和Server后端开发、风险分析算法设计、前端界面开发、测试用例编写和论文撰写等。

======================================
第二部分：eBPF技术原理 (11-20)
======================================

11. 问：什么是eBPF？它与传统BPF有什么区别？
答：eBPF（Extended Berkeley Packet Filter）是Linux内核的一项革命性技术，允许用户在内核空间安全地执行自定义程序。与传统BPF的区别：（1）从仅网络包过滤扩展到通用内核编程；（2）支持更复杂的指令集和数据结构；（3）提供JIT编译器，性能接近原生代码；（4）提供了验证器确保程序安全。

12. 问：eBPF程序是如何加载到内核的？
答：加载过程包括：（1）使用Clang/LLVM将C代码编译为eBPF字节码；（2）通过bpf()系统调用将字节码加载到内核；（3）内核验证器检查程序安全性；（4）JIT编译器将字节码编译为机器码；（5）将程序附加到特定的hook点（如tracepoint、kprobe等）。

13. 问：eBPF的验证器起什么作用？
答：eBPF验证器是内核安全的关键组件，主要功能包括：（1）确保程序不会访问越界内存；（2）检查程序会在有限时间内结束（无无限循环）；（3）验证程序不会调用未授权的内核函数；（4）确保类型安全。只有通过验证的eBPF程序才能在内核中运行。

14. 问：项目中使用了哪些eBPF map类型？
答：项目中使用了BPF_MAP_TYPE_RINGBUF类型的map。Ring buffer（环形缓冲区）是一种高效的队列结构，适合高频事件的传递。相比perf buffer，ring buffer支持更大的数据包、更灵活的内存管理，并且避免了perf buffer在高负载下的事件丢失问题。

15. 问：为什么选择ring buffer而不是perf buffer？
答：选择ring buffer的原因：（1）支持变长数据包，更灵活；（2）内存管理更高效，预先分配内存；（3）在高负载下性能更好，不易丢失事件；（4）API更简洁，使用体验更好；（5）是Linux 5.8+推荐的事件传递方式。

16. 问：项目中使用了哪些eBPF hook点？
答：使用了以下hook点：（1）Tracepoint：sys_enter_execve（监控进程创建）、sys_enter_setuid（监控权限变更）、sys_enter_openat（监控文件打开）、sys_enter_connect（监控网络连接）；（2）LSM Hook：task_fix_setuid（监控UID变更）、file_permission（监控文件权限检查）。

17. 问：Tracepoint和Kprobe有什么区别？
答：区别包括：（1）Tracepoint是内核开发者预定义的稳定监控点，ABI保证向后兼容；（2）Kprobe可以动态探测任意内核函数，但可能因内核版本变化而失效；（3）Tracepoint性能开销略低，上下文信息更丰富；（4）本项目主要使用Tracepoint以确保稳定性。

18. 问：LSM Hook在项目中起什么作用？
答：LSM（Linux Security Modules）Hook是内核安全框架的扩展点。项目中的LSM Hook用于：（1）task_fix_setuid：捕获权限变更的决策点，可以检测setuid等提权操作；（2）file_permission：在文件权限检查时触发，可以检测对敏感文件的操作。相比系统调用hook，LSM Hook可以在更底层进行监控。

19. 问：eBPF程序在用户空间和内核空间如何通信？
答：通过eBPF Map进行通信：（1）内核空间eBPF程序将事件写入ring buffer map；（2）用户空间Agent通过ring buffer reader读取事件；（3）通信是单向的，内核向用户传递数据；（4）使用共享内存机制，避免了系统调用开销，性能高效。

20. 问：eBPF程序执行过程中有哪些限制？
答：主要限制包括：（1）程序大小限制（指令数限制）；（2）不允许使用无限循环；（3）不能直接访问用户空间内存，需使用helper函数；（4）不能调用某些危险内核函数；（5）栈空间有限。这些限制保证了eBPF程序的安全性和可控性。

======================================
第三部分：系统架构与设计 (21-30)
======================================

21. 问：请画出系统的整体架构图。
答：系统采用三层架构：（1）数据采集层：Agent运行eBPF程序，从内核采集事件；（2）数据处理层：Server接收事件，进行风险分析和存储；（3）数据展示层：前端仪表板提供可视化界面。数据流向为：内核→eBPF→Agent→Server→数据库→前端。

22. 问：为什么采用Agent-Server架构而不是单体应用？
答：采用分布式架构的原因：（1）监控目标可能分布在多台主机，Agent可以部署在不同节点；（2）解耦监控和展示，便于扩展；（3）Server可以集中管理多个Agent的事件，提供统一视图；（4）便于未来扩展集群监控能力。

23. 问：Agent和Server之间使用什么协议通信？
答：使用HTTP/REST API进行通信，Agent通过POST请求将事件发送到Server的/api/events端点。选择HTTP的原因包括：简单易用、跨平台、便于调试、支持JSON格式传输。

24. 问：事件数据结构是如何设计的？
答：事件结构体包含：（1）基本信息：时间戳、PID、PPID、UID、GID；（2）权限信息：EUID、EGID、旧UID、新UID；（3）进程信息：命令名、执行文件名；（4）事件类型：执行、文件操作、权限变更、网络连接等；（5）网络信息：源/目的地址和端口；（6）事件特征：是否提权、目标文件类型等。

25. 问：数据库设计是怎样的？
答：使用SQLite数据库，主要表包括events表，字段包括：主键ID、时间戳、PID、PPID、权限信息、命令名、事件类型、风险等级、事件指纹（用于去重）、Agent ID、创建时间等。SQLite轻量级，无需额外服务，适合中小规模部署。

26. 问：为什么选择Go语言开发Agent和Server？
答：选择Go语言的原因：（1）性能好，编译型语言，执行效率高；（2）并发模型优秀，适合高并发场景；（3）静态类型，减少运行时错误；（4）有完善的eBPF库支持（cilium/ebpf）；（5）部署简单，单文件即可运行；（6）跨平台编译方便。

27. 问：系统的并发处理是如何设计的？
答：并发处理体现在多个层面：（1）eBPF层面：在内核中异步收集事件；（2）Agent层面：每个事件使用goroutine发送到Server；（3）并发控制：使用信号量限制最多50个并发请求，避免压垮Server；（4）Server层面：使用sync.RWMutex保护共享数据，WebSocket广播使用独立goroutine。

28. 问：事件去重是如何实现的？
答：基于事件指纹去重：（1）为每个事件生成唯一指纹（包含PID、时间戳、事件类型等）；（2）接收事件时查询数据库是否存在相同指纹；（3）如已存在则直接返回成功，避免重复存储和处理。去重减少了存储压力和告警噪音。

29. 问：风险等级是如何定义的？
答：风险等级分为三级：（1）LOW：常规系统行为，如普通文件读写；（2）MEDIUM：可疑行为，如访问敏感文件；（3）HIGH：高危行为，如权限提升、反向shell连接。评估基于事件类型、权限变化、文件敏感性等多维度特征。

30. 问：系统的可扩展性如何？
答：系统具备良好的可扩展性：（1）可以方便地添加新的eBPF hook点监控新类型事件；（2）风险分析引擎可插拔，支持自定义分析规则；（3）支持多Agent部署，可扩展到集群监控；（4）后端API设计RESTful，便于集成其他系统；（5）数据库可升级为MySQL/PostgreSQL等。

======================================
第四部分：安全检测机制 (31-45)
======================================

31. 问：系统如何检测权限提升攻击？
答：通过以下机制检测：（1）监控setuid系统调用和task_fix_setuid LSM hook；（2）比对进程的UID和EUID，如不一致则标记为提权；（3）记录提权前后的UID变化；（4）结合进程上下文（命令名、父进程）判断是否为异常提权；（5）高风险提权事件会触发告警。

32. 问：如何检测反向shell攻击？
答：检测逻辑：（1）识别常见shell进程（bash、sh、zsh、dash）；（2）监控这些进程发起的TCP出站连接；（3）结合进程上下文，如父进程是否为可疑程序；（4）检查连接目标是否为外部地址；（5）异常的shell网络连接被标记为反向shell风险。

33. 问：系统如何识别敏感文件访问？
答：敏感文件识别机制：（1）定义敏感文件列表：/etc/passwd、/etc/shadow、/etc/sudoers、SSH配置、定时任务文件等；（2）监控openat系统调用和file_permission LSM hook；（3）匹配被访问文件路径；（4）根据文件类型分类（密码文件、配置文件等）；（5）敏感文件访问被赋予中等或高风险等级。

34. 问：系统如何检测SUID程序的异常使用？
答：检测方法：（1）监控execve系统调用，记录执行的文件路径；（2）结合EUID信息，识别以提升权限运行的程序；（3）检查执行的文件是否设置了SUID位；（4）分析命令参数，识别可疑的SUID利用模式；（5）如普通用户执行SUID程序且行为异常，触发告警。

35. 问：网络连接监控的重点是什么？
答：重点监控：（1）出站连接：可能的数据外泄或命令执行；（2）Shell进程的网络连接：反向shell特征；（3）可疑工具（nc、netcat、telnet）的连接；（4）Root权限进程的网络活动；（5）连接到非标准端口的流量。

36. 问：系统如何防止被攻击者绕过？
答：防范措施：（1）内核级监控，用户态难以绕过；（2）使用LSM Hook在安全决策点监控，更难规避；（3）不依赖日志文件，直接从内核获取信息；（4）监控多个维度，单一绕过难以完全隐藏；（5）可以配合其他安全措施形成纵深防御。

37. 问：系统的误报率如何控制？
答：控制误报的措施：（1）风险等级分级，高风险才告警；（2）结合上下文信息综合判断；（3）事件去重避免重复告警；（4）白名单机制，过滤已知安全行为；（5）提供人工审核接口，管理员可标记误报。

38. 问：如何检测进程注入攻击？
答：检测思路：（1）监控execve系统调用，记录进程父子关系；（2）检查子进程的命令行参数是否异常；（3）监控/proc下的进程内存映射变化；（4）检测ptrace等调试工具的使用；（5）异常的进程创建模式（如常见守护进程的异常子进程）。

39. 问：系统如何应对Rootkit攻击？
答：应对策略：（1）内核级监控，Rootkit难以完全隐藏；（2）监控内核模块加载（未来可扩展）；（3）比对用户态和内核态的进程信息；（4）监控敏感系统调用序列；（5）结合其他安全工具交叉验证。

40. 问：如何检测文件篡改？
答：检测方法：（1）监控文件的打开和写入操作；（2）记录文件的修改时间戳；（3）可以集成文件完整性监控（FIM）；（4）检测对系统关键文件（如/etc/hosts、/etc/passwd）的修改；（5）异常时间或异常用户的文件修改触发告警。

41. 问：系统如何处理加密流量？
答：当前限制：（1）eBPF监控的是连接建立阶段，不检查载荷；（2）可以记录连接的元数据（IP、端口、时间）；（3）对于加密流量，结合其他信息（进程上下文、连接模式）判断风险；（4）未来可以集成深度包检测（DPI）技术。

42. 问：系统如何检测端口扫描行为？
答：检测逻辑：（1）监控connect和bind系统调用；（2）分析同一进程对多个不同端口的连接尝试；（3）短时间内大量连接失败的模式识别；（4）扫描工具的命令行特征；（5）异常的端口访问序列。

43. 问：如何检测定时任务后门？
答：检测方法：（1）监控对crontab和定时任务目录的写入操作；（2）记录定时任务文件的修改内容；（3）检测异常的定时任务创建（如非常规时间执行）；（4）监控定时任务执行时的行为；（5）关联分析定时任务和恶意进程。

44. 问：系统如何应对容器环境？
答：容器支持：（1）eBPF可以监控容器内的进程（需要适当的权限）；（2）可以识别容器的命名空间信息；（3）支持跨容器监控，提供统一视图；（4）需要配置适当的权限和资源限制；（5）未来可以集成容器运行时接口。

45. 问：系统如何检测DDoS攻击的发起端？
答：检测思路：（1）监控大量出站连接的进程；（2）识别网络攻击工具的使用；（3）检测异常的流量模式；（4）分析进程的CPU和网络使用情况；（5）结合系统资源使用率判断是否被用作攻击节点。

======================================
第五部分：性能优化 (46-55)
======================================

46. 问：系统的性能开销有多大？
答：性能开销较小：（1）eBPF程序在内核中执行，接近原生代码性能；（2）主要开销在事件传输和处理，经测试CPU使用率增加约2-5%；（3）内存开销主要在ring buffer和数据库，可配置；（4）网络开销取决于事件频率，可通过采样和过滤控制。

47. 问：如何优化eBPF程序的执行效率？
答：优化措施：（1）减少eBPF程序中的复杂逻辑，简单判断后立即返回；（2）使用early return，避免不必要的计算；（3）合理使用map，减少内核和用户空间通信；（4）限制监控的事件类型，过滤无关事件；（5）使用高效的字符串比较方法。

48. 问：Agent如何避免成为性能瓶颈？
答：避免瓶颈的方法：（1）使用goroutine异步发送事件；（2）控制并发请求数量（信号量限制为50）；（3）增加HTTP超时时间，避免长时间阻塞；（4）过滤Agent自身的事件，避免无限循环；（5）使用连接池复用HTTP连接。

49. 问：数据库查询如何优化？
答：优化策略：（1）为常用查询字段添加索引（时间戳、PID、风险等级）；（2）使用分页查询，避免一次返回大量数据；（3）限制查询结果数量，最多1000条；（4）使用内存缓存热点数据；（5）定期清理历史数据，控制数据库大小。

50. 问：如何处理高并发场景？
答：高并发处理：（1）使用带缓冲的channel控制并发度；（2）数据库连接池管理；（3）读写锁保护共享数据；（4）WebSocket广播使用独立goroutine，不阻塞请求处理；（5）可以使用消息队列（如Kafka）解耦采集和处理。

51. 问：事件丢失如何处理？
答：防丢措施：（1）使用ring buffer，避免perf buffer的丢失问题；（2）增大ring buffer大小（当前256KB）；（3）监控ring buffer使用率，及时告警；（4）关键事件可以持久化到本地文件；（5）Server端支持重试机制。

52. 问：系统的资源占用如何？
答：资源占用情况：（1）CPU：正常负载下增加2-5%，高事件场景可能达到10-15%；（2）内存：Agent约50-100MB，Server约100-200MB，数据库根据事件数量增长；（3）磁盘：日志和数据库文件，可通过配置控制；（4）网络：取决于事件频率，可压缩传输。

53. 问：如何降低网络传输开销？
答：降低开销的方法：（1）使用JSON压缩（如gzip）；（2）批量发送事件，减少请求次数；（3）过滤低风险事件，减少传输量；（4）使用二进制协议替代JSON；（5）本地预处理，只传输必要信息。

54. 问：系统如何适应不同负载？
答：自适应机制：（1）动态调整采样率，高负载时降低监控精度；（2）根据系统资源使用情况自动降级；（3）可配置的事件类型白名单，按需监控；（4）支持水平扩展，增加Server实例；（5）提供性能指标，便于调优。

55. 问：如何进行性能测试？
答：测试方法：（1）使用压力测试工具（如ab、wrk）模拟高并发；（2）生成模拟事件，测试极限吞吐量；（3）监控CPU、内存、网络使用率；（4）测量事件从采集到展示的端到端延迟；（5）对比开启/关闭监控的系统性能差异。

======================================
第六部分：测试与验证 (56-65)
======================================

56. 问：你如何测试系统的功能正确性？
答：测试方法：（1）单元测试：测试各个函数和模块；（2）集成测试：测试Agent-Server-数据库的协作；（3）场景测试：模拟攻击场景（提权、反向shell等）验证检测能力；（4）回归测试：确保修改不影响现有功能；（5）手动测试：验证前端界面和API。

57. 问：项目中写了哪些测试脚本？
答：测试脚本包括：（1）test_all_events.sh：测试所有类型事件的采集；（2）test_attack_detection.sh：测试攻击检测功能；（3）test_privilege_escalation.sh：测试权限提升检测；（4）test_openat_connect.sh：测试文件操作和网络连接监控；（5）test_risk_levels.sh：测试风险等级评估。

58. 问：如何测试攻击检测的准确性？
答：测试方法：（1）构造真实的攻击场景（如使用sudo提权、nc建立反向shell）；（2）验证系统能否正确识别并告警；（3）测试误报情况，运行正常程序验证无告警；（4）统计检测率和误报率；（5）使用攻击样本库进行批量测试。

59. 问：系统测试的覆盖率如何？
答：测试覆盖情况：（1）eBPF程序：测试了所有事件类型的采集；（2）Agent：测试了事件发送、过滤、错误处理；（3）Server：测试了API、数据库操作、风险分析；（4）前端：测试了页面展示、查询功能；（5）整体：覆盖了主要功能路径和异常场景。

60. 问：你如何验证系统不会影响业务正常运行？
答：验证方法：（1）在测试环境先部署，观察性能影响；（2）逐步开启监控功能，分阶段验证；（3）监控系统资源使用，确保在可接受范围；（4）保留关闭监控的快速通道；（5）与业务方沟通，获得反馈。

61. 问：压力测试的结果如何？
答：压力测试结果：（1）可处理每秒1000+事件；（2）并发50个请求时响应时间<100ms；（3）数据库查询1000条记录<50ms；（4）内存使用稳定，无明显泄漏；（5）长时间运行（24小时+）无崩溃。

62. 问：如何测试系统的稳定性？
答：稳定性测试：（1）长时间运行测试，观察内存泄漏和性能衰减；（2）异常输入测试，验证错误处理；（3）网络中断测试，验证重连机制；（4）数据库故障测试，验证降级处理；（5）高负载测试，验证极限情况下的表现。

63. 问：兼容性测试做了哪些？
答：兼容性测试：（1）不同Linux内核版本（5.10、5.15、6.x）；（2）不同发行版（Ubuntu、CentOS、Debian）；（3）不同硬件架构（x86_64、ARM64）；（4）不同Go版本；（5）测试eBPF功能在不同环境下的可用性。

64. 问：如何测试取证报告功能？
答：测试方法：（1）生成模拟攻击事件；（2）查询指定事件的时间线；（3）生成取证报告，验证内容完整性；（4）检查报告格式（JSON、Text、Markdown）；（5）验证报告中的时间戳、进程关系、风险分析等信息的准确性。

65. 问：用户体验测试如何进行？
答：测试方法：（1）邀请同学使用系统，收集反馈；（2）测试页面加载速度和交互流畅度；（3）验证查询功能的易用性；（4）检查告警信息的清晰度；（5）评估仪表板的信息展示效果。

======================================
第七部分：前端与可视化 (66-75)
======================================

66. 问：前端使用了哪些技术？
答：前端技术：（1）HTML5：页面结构；（2）CSS3：样式设计；（3）JavaScript：交互逻辑；（4）Fetch API：与Server通信；（5）WebSocket：实时事件推送；（6）Chart.js：数据可视化（图表）。

67. 问：前端如何实时获取事件？
答：实时获取方式：（1）使用WebSocket建立持久连接；（2）Server有新事件时通过WebSocket推送到前端；（3）前端接收事件后动态更新页面；（4）支持断线重连机制；（5）初始加载时获取最近100个事件。

68. 问：仪表板展示了哪些信息？
答：展示内容包括：（1）实时事件列表：时间、PID、命令、事件类型、风险等级；（2）统计信息：事件总数、各类型事件数量、风险分布；（3）过滤功能：按风险等级、事件类型、时间范围筛选；（4）事件详情：点击查看完整信息；（5）取证报告入口。

69. 问：如何优化前端性能？
答：优化措施：（1）使用虚拟滚动，避免渲染大量DOM；（2）事件列表只显示摘要，详情按需加载；（3）使用防抖和节流控制更新频率；（4）静态资源使用CDN或缓存；（5）减少不必要的重绘和回流。

70. 问：前端如何处理高频率事件？
答：处理策略：（1）限制显示的事件数量（最多100条）；（2）使用时间窗口，定期刷新列表；（3）高负载时降低更新频率；（4）提供暂停/恢复按钮；（5）使用分页或懒加载。

71. 问：图表可视化是如何实现的？
答：实现方式：（1）使用Chart.js库绘制图表；（2）饼图展示风险等级分布；（3）柱状图展示事件类型统计；（4）折线图展示事件时间趋势；（5）数据从API获取，定期更新。

72. 问：前端如何适配不同设备？
答：适配方法：（1）使用响应式设计，CSS媒体查询；（2）移动端简化显示，突出关键信息；（3）使用弹性布局（Flexbox/Grid）；（4）字体大小自适应；（5）提供移动端优化的交互方式。

73. 问：前端的安全性如何保证？
答：安全措施：（1）使用HTTPS加密通信（生产环境）；（2）输入验证，防止XSS攻击；（3）CORS配置，限制跨域访问；（4）会话管理，防止CSRF攻击；（5）敏感信息不在前端存储。

74. 问：用户体验方面有哪些优化？
答：优化内容：（1）使用loading提示，提升等待体验；（2）事件颜色编码（红色高风险、黄色中风险、绿色低风险）；（3）提供搜索和过滤功能，快速定位事件；（4）使用图标增强可读性；（5）支持导出数据为CSV/JSON。

75. 问：前端未来可以如何改进？
答：改进方向：（1）使用现代框架（Vue/React）重构；（2）增加更多图表类型和交互；（3）支持自定义仪表板布局；（4）增加告警通知功能；（5）支持多语言国际化。

======================================
第八部分：部署与运维 (76-85)
======================================

76. 问：系统如何部署？
答：部署步骤：（1）编译eBPF程序：clang -O2 -g -target bpf -c；（2）编译Agent：go build；（3）编译Server：go build；（4）启动Server：nohup ./server &；（5）启动Agent：sudo ./agent；（6）访问前端：http://server:8080/static/dashboard.html。

77. 问：系统有什么依赖？
答：依赖包括：（1）内核：Linux 5.8+（支持ring buffer）；（2）工具：clang、llvm、libbpf；（3）Go：1.16+；（4）权限：需要CAP_BPF、CAP_SYS_ADMIN等；（5）可选：Docker用于容器化部署。

78. 问：如何配置系统参数？
答：配置方法：（1）修改server/main.go中的数据库路径；（2）调整Agent中的serverURL；（3）修改eBPF程序中的ring buffer大小；（4）配置日志级别和输出路径；（5）调整并发控制参数（如信号量大小）。

79. 问：系统如何进行日志管理？
答：日志管理：（1）Agent日志输出到agent.log；（2）Server日志输出到server.log；（3）支持日志轮转，避免文件过大；（4）日志级别可配置（DEBUG、INFO、WARN、ERROR）；（5）可以集成到ELK等日志分析系统。

80. 问：如何监控系统运行状态？
答：监控方式：（1）使用/api/health端点检查健康状态；（2）监控进程存活（systemd、supervisor）；（3）检查CPU、内存使用率；（4）监控事件采集速率；（5）设置告警规则，异常时通知。

81. 问：系统如何备份和恢复？
答：备份策略：（1）定期备份SQLite数据库文件；（2）备份配置文件；（3）备份eBPF程序和可执行文件；（4）恢复时停止服务，恢复文件后重启；（5）可以编写自动化备份脚本。

82. 问：如何升级系统？
答：升级步骤：（1）停止Agent和Server；（2）备份当前版本和数据；（3）替换可执行文件；（4）启动新版本Server，验证兼容性；（5）启动新版本Agent，观察日志；（6）如失败，回滚到备份版本。

83. 问：系统如何处理权限问题？
答：权限管理：（1）Agent需要root权限加载eBPF；（2）可以通过capabilities精细化授权；（3）Server可以普通用户运行；（4）建议使用专用服务账户；（5）定期审计权限配置。

84. 问：如何进行故障排查？
答：排查步骤：（1）检查进程是否运行；（2）查看日志文件定位错误；（3）使用dmesg查看内核日志；（4）检查网络连接和端口；（5）使用bpftool检查eBPF程序状态；（6）验证数据库连接。

85. 问：系统如何容器化部署？
答：容器化方法：（1）编写Dockerfile，包含所有依赖；（2）挂载必要的系统目录（/sys/kernel/debug等）；（3）授予必要的capabilities（CAP_BPF等）；（4）使用docker-compose编排多容器；（5）注意网络和存储的持久化配置。

======================================
第九部分：项目挑战与解决 (86-95)
======================================

86. 问：开发过程中遇到的最大挑战是什么？
答：最大挑战是eBPF程序的调试和优化。由于eBPF在内核中运行，调试困难，只能通过打印到ring buffer的方式调试。此外，需要理解内核数据结构，正确读取进程信息，同时要符合eBPF验证器的限制，这些都需要大量的调试和学习。

87. 问：如何解决Agent和Server连接超时的问题？
答：解决方案：（1）增加HTTP超时时间（5秒→10秒）；（2）过滤Agent自身的事件，避免无限循环；（3）添加并发控制，限制请求数量；（4）增加错误重试机制；（5）优化Server处理逻辑，提高响应速度。

88. 问：如何处理eBPF验证器的限制？
答：应对方法：（1）简化程序逻辑，避免复杂嵌套；（2）使用helper函数代替直接内存访问；（3）正确使用map进行数据交换；（4）避免无限循环；（5）参考官方示例和文档，理解验证器规则。

89. 问：如何解决不同内核版本的兼容性问题？
答：兼容性方案：（1）使用条件编译，针对不同内核版本；（2）检查内核特性是否可用（如ring buffer）；（3）提供降级方案（如不支持ring buffer则用perf buffer）；（4）测试多个内核版本；（5）文档说明最低内核版本要求。

90. 问：如何平衡监控深度和性能？
答：平衡策略：（1）监控关键事件类型，避免全量采集；（2）在eBPF中做初步过滤，减少用户空间处理；（3）使用采样率控制高频事件；（4）提供配置选项，用户可根据需求调整；（5）实时监控系统性能，动态调整策略。

91. 问：如何确保取证报告的准确性？
答：保证措施：（1）从内核直接获取信息，不依赖用户态；（2）使用原子操作，避免并发问题；（3）记录完整的事件上下文；（4）使用数据库事务保证数据一致性；（5）多轮测试验证报告的正确性。

92. 问：如何处理大量历史数据的查询性能？
答：优化方法：（1）为查询字段建立索引；（2）使用分区表，按时间分区；（3）定期归档旧数据；（4）提供时间范围查询，避免全表扫描；（5）可以使用更强大的数据库（如PostgreSQL）替代SQLite。

93. 问：如何解决安全误报问题？
答：改进方向：（1）完善风险分析算法，增加更多上下文判断；（2）支持白名单，过滤已知安全行为；（3）提供机器学习模型，动态调整风险阈值；（4）支持人工标注，持续优化检测规则；（5）提供详细的误报分析工具。

94. 问：如何提升系统的易用性？
答：改进措施：（1）提供一键部署脚本；（2）编写详细的使用文档；（3）提供配置模板，降低配置复杂度；（4）优化前端界面，提升交互体验；（5）提供API文档和示例代码。

95. 问：项目开发中获得了哪些经验教训？
答：经验教训：（1）深入理解底层技术（eBPF）的重要性；（2）测试驱动开发，及早发现问题；（3）文档的重要性，便于维护和交接；（4）性能测试不能忽视；（5）与导师和同学多交流，获得反馈和建议。

======================================
第十部分：未来工作与展望 (96-100)
======================================

96. 问：这个项目有哪些可以改进的地方？
答：改进方向：（1）增加更多攻击检测规则（如内存篡改、内核模块加载）；（2）支持容器和云环境监控；（3）引入机器学习，提高检测准确率；（4）支持分布式部署，监控大规模集群；（5）增强告警机制（邮件、短信、钉钉通知）。

97. 问：未来可以扩展哪些功能？
答：扩展功能：（1）支持行为基线，检测异常偏离；（2）集成威胁情报，识别已知攻击者；（3）提供自动响应功能（阻断可疑进程）；（4）支持自定义检测规则；（5）提供API供其他系统集成；（6）支持跨平台（Windows、macOS）。

98. 问：系统可以应用到哪些实际场景？
答：应用场景：（1）企业内网安全监控；（2）云主机入侵检测；（3）DevSecOps流水线集成；（4）安全合规审计；（5）应急响应和取证；（6）网络安全研究和教学；（7）关键基础设施保护。

99. 问：这个项目对你个人有什么收获？
答：个人收获：（1）深入学习了eBPF技术和Linux内核；（2）掌握了Go语言开发后端服务；（3）提升了系统设计和架构能力；（4）增强了安全攻防知识；（5）锻炼了独立解决问题的能力；（6）积累了项目管理和文档撰写经验。

100. 问：你对这个项目的未来发展有什么规划？
答：未来规划：（1）继续优化核心检测算法；（2）增加更多的攻击场景覆盖；（3）支持更复杂的分布式架构；（4）开源项目，获得社区反馈；（5）与实际生产环境结合，进行实地验证；（6）发表论文或技术博客，分享经验；（7）考虑商业化或应用到实习工作中。

======================================
答辩准备建议
======================================

1. 熟悉项目的每一个模块和功能，能够清晰阐述设计思路
2. 准备演示环境，现场展示系统能力
3. 预测可能的追问，准备技术细节的解释
4. 突出项目的创新点和难点
5. 保持自信，诚实回答，不懂的问题可以说需要进一步研究
6. 准备简洁有力的开场白（2-3分钟）
7. 制作精美的PPT，突出重点
8. 准备代码示例，展示关键技术实现
9. 了解相关领域的前沿动态
10. 感谢导师和帮助过的人

祝答辩顺利！